# Vision — Cycling Form & Performance Analyzer

Stub placeholder. The real README is created by a later Phase 0 plan
(see `.planning/phases/00-bootstrap-cost-guardrails/`). This file exists so
`pyproject.toml`'s `readme = "README.md"` reference resolves during builds.
