# Cloud Function runtime dependencies for the billing kill switch.
# Vendored from Cyclenerd/poweroff-google-cloud-cap-billing
# (upstream commit e5781791e08b86cb49debffce82bace453b1e809).
#
# NOTE: these pins target the Cloud Functions Gen 2 Python 3.12 runtime —
# they are NOT installed into the local dev venv.
#
# Upstream uses google-cloud-billing (the modern, idiomatic client for the
# Cloud Billing API) — its UpdateProjectBillingInfoRequest call is the
# Python-client equivalent of the REST method
# `cloudbilling.projects.updateBillingInfo`.
google-cloud-billing==1.19.0

# google-auth is pulled in transitively by google-cloud-billing, but pinning
# it explicitly keeps the Cloud Function's dependency graph stable across
# minor upstream releases.
google-auth==2.35.0
