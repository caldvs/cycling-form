---
phase: 00-bootstrap-cost-guardrails
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/filming-protocol.md
autonomous: true
requirements:
  - BOOT-05
tags:
  - docs
  - filming
  - cv

must_haves:
  truths:
    - "`docs/filming-protocol.md` exists at the documented path"
    - "Document locks all four hard invariants: camera height = BB height (±2 cm), fiducial in frame, 60fps capture, CFR, tripod-only"
    - "Document contains a labeled ASCII side-view diagram showing camera placement relative to the bottom bracket and a fiducial in frame"
    - "Document includes a 6-item pre-shot checklist the operator runs through the phone preview before pressing record"
    - "Document includes a one-paragraph lighting guidance (avoid backlit, prefer 5000K LED)"
  artifacts:
    - path: "docs/filming-protocol.md"
      provides: "Filming-protocol one-pager — locks the geometry every downstream pose phase depends on"
      contains: "BB height"
      min_lines: 50
  key_links:
    - from: "docs/filming-protocol.md"
      to: "ROADMAP Phase 0 success criterion #4"
      via: "explicit listing of the four hard locks (camera height = BB height, fiducial, 60fps, CFR, tripod-only)"
      pattern: "60fps|60 fps"
    - from: "docs/filming-protocol.md"
      to: "Pitfall #1 (camera-rig sloppiness)"
      via: "fiducial-in-frame + drift-check protocol"
      pattern: "fiducial"
    - from: "docs/filming-protocol.md"
      to: "Pitfall #8 (FPS aliasing + VFR)"
      via: "60fps + CFR mandates"
      pattern: "CFR|constant frame rate"
---

<objective>
Author `docs/filming-protocol.md` — a concise one-pager that locks the geometric invariants every downstream pose phase depends on. Pitfall #1 (camera-rig sloppiness) and Pitfall #8 (FPS aliasing + VFR) are the two failure modes this document prevents. Without an airtight protocol, every joint angle computed in Phase 1 has unknown bias; the headline correlations would be noise dressed up as signal.

Purpose: BOOT-05. Phase 0 success criterion #4. The protocol is one of the JD-signal-bearing deliverables for the CV/pose bullet at Phase 0 (per CONTEXT §JD signal coverage).

Output: A single markdown file that an operator (the user) can read in 60 seconds and execute without ambiguity before every fixture-ride filming session.
</objective>

<execution_context>
@/Users/callum/software-local/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callum/software-local/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/callum/software-local/vision/.planning/STATE.md
@/Users/callum/software-local/vision/.planning/ROADMAP.md
@/Users/callum/software-local/vision/.planning/REQUIREMENTS.md
@/Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md
@/Users/callum/software-local/vision/.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/filming-protocol.md (one-pager + ASCII side-view diagram + checklist)</name>
  <files>docs/filming-protocol.md</files>
  <read_first>
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-15..D-17 (concise one-pager, 6-item checklist, ASCII diagram, four hard locks, lighting paragraph; `filming-protocol-bad-examples.md` is DEFERRED to Phase 1)
    - /Users/callum/software-local/vision/.planning/research/PITFALLS.md §#1 (side-view camera-rig sloppiness — warning signs)
    - /Users/callum/software-local/vision/.planning/research/PITFALLS.md §#8 (FPS aliasing + VFR — referenced earlier; the 60fps + CFR mandate)
  </read_first>
  <action>
    Create `docs/filming-protocol.md` with the following sections in this order. Use H1 for the title, H2 for top-level sections, H3 only where needed. Keep total length ≤120 lines including the diagram (concise one-pager per D-15).

    ## Section 1: Title + one-paragraph purpose
    ```
    # Filming Protocol for Vision

    This protocol locks the geometry every downstream pose-and-correlation
    stage depends on. The four hard invariants (camera at BB height, fiducial
    in frame, 60fps, constant frame rate, tripod) are non-negotiable. A clip
    that violates any of them is thrown out — re-shoot, do not "fix it in
    post." Pitfall #1 (camera-rig sloppiness) and Pitfall #8 (FPS aliasing
    and Variable Frame Rate) are why this document exists.
    ```

    ## Section 2: The four hard locks
    Numbered list with one bullet per lock, each with a one-line "why" reference to the pitfall:

    1. **Camera height = bottom-bracket (BB) height ±2 cm.** Mounted on a tripod, lens dead-level (not tilted up or down). _Why: a tilted camera introduces non-uniform foreshortening across the pedal stroke that biases hip + knee angles per-frame (Pitfall #1)._
    2. **Fiducial visible in frame at all times.** A vertical plumb-line or printed ChArUco/AprilTag marker taped to the wall behind the trainer, visible in every frame. _Why: if the fiducial drifts >2 px across the clip, the camera moved and every angle is wrong._
    3. **60 fps capture (minimum), constant frame rate (CFR), never VFR.** Use the device's "Cinematic" or "Manual 60fps" mode; iPhone/Android default video is often VFR and silently varies between 24–60fps. _Why: VFR breaks the time alignment between video frames and FIT records; 60fps is required for stroke-segmentation peak detection to resolve TDC/BDC (Pitfall #8)._
    4. **Tripod only — no handheld, no propped-against-a-chair.** Even a 1 mm camera drift over 60 minutes destroys the stationarity assumption. _Why: every downstream KOPS measurement is computed in camera-pixel space and assumes the camera is fixed._

    ## Section 3: Side-view ASCII diagram
    Embed this exact block inside a fenced code block (use triple-backticks with no language). The diagram must show: camera tripod to the left, rider on bike to the right, BB-height level line, fiducial behind the rider:

    ```
                                      wall
                                       |
                                       |  ╔═══════╗
                                       |  ║ ←─────╫─── fiducial
                                       |  ║       ║   (plumb-line OR
                                       |  ║       ║    AprilTag marker)
                                       |  ╚═══════╝
                                       |
              tripod                   |
              ┌──────┐                 |              rider on
              │ cam  │═══════════════  | ◯ ←─── BB    indoor trainer
              │ ↕    │   level horiz.  | ╱ ╲          (side-on)
              └──────┘                 |╱   ╲
                  │   ↑                |
                  │   │                |
                  │   │ camera lens = BB height ±2 cm
                  │   │
                  │   ▼
                ═════════════════ floor ═════════════════
                  ↑                                       ↑
                  └──────── ~2–3 m distance ──────────────┘
                  (rider fully in frame from helmet to ankle at BDC)

    LEGEND:  ◯ = bottom bracket   ↕ = camera adjustable height
             ║ = wall-mounted fiducial behind the rider
    ```

    Below the diagram, one short paragraph:
    > Distance camera-to-bike is ~2–3 m so the rider's helmet (at peak of stroke) and the ankle (at BDC, full extension) both stay inside the frame. Use a 35 mm-equivalent or wider focal length — phones default to ~26 mm-equivalent on the main lens, which is fine.

    ## Section 4: Pre-shot checklist (six items)
    Numbered checklist the operator runs through the phone preview before pressing record:

    1. [ ] Tripod legs locked; bubble level on the tripod head reads centered.
    2. [ ] Camera lens dead-level (use the phone's grid + level overlay).
    3. [ ] Camera height equals BB height (measure with a tape, ±2 cm tolerance).
    4. [ ] Fiducial visible in the phone preview AND in the bottom-third of the frame so it isn't cropped.
    5. [ ] Frame rate set to 60fps (or 120fps if conditions allow); video format set to a CFR mode (NOT "auto" / "smart" / "low-light boost", which silently switch to VFR).
    6. [ ] Rider fully in frame at both top of stroke (TDC) and bottom of stroke (BDC) — pedal a quick test stroke and verify.

    ## Section 5: Lighting (one paragraph)
    > Prefer diffuse 5000 K (daylight) LED from the camera side; avoid backlit setups (window behind the rider washes out the rider silhouette and confuses MediaPipe's edge detection). A single ceiling LED panel above and slightly behind the camera is the cheapest acceptable setup. Mixed-temperature lighting (warm bedside lamp + cool window) is fine but reduces pose-estimator visibility scores at the joint extremes.

    ## Section 6: Verification after recording (3 quick checks)
    Three bullets — what to verify in the clip itself before committing it as a fixture ride:
    - Run `ffprobe -v error -show_streams -select_streams v:0 <file>.mp4 | grep -E 'r_frame_rate|avg_frame_rate|nb_frames|duration'`. The `r_frame_rate` and `avg_frame_rate` MUST be identical (e.g., both `60/1`). If they differ, the clip is VFR — reject it (Pitfall #8).
    - Scrub the timeline by hand. The fiducial should be stationary frame-to-frame. If it drifts laterally, the tripod settled; throw the clip out.
    - At TDC and BDC, the rider should be fully in frame. If the helmet or ankle clips out, re-frame and re-shoot.

    ## Section 7: References (one short H2)
    - `.planning/research/PITFALLS.md` §Pitfall #1 (camera-rig sloppiness)
    - `.planning/research/PITFALLS.md` §Pitfall #8 (FPS aliasing + VFR)
    - `.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md` §D-15..D-17

    NOTE: Bad-example annotated frames are explicitly NOT part of this document (D-17 defers them to Phase 1 when real fixture rides exist).

    Verification step: count the lines (`wc -l docs/filming-protocol.md`) and confirm ≥50 lines (must-haves min_lines) and ≤120 lines (D-15 concise one-pager intent).
  </action>
  <verify>
    <automated>cd /Users/callum/software-local/vision && test -f docs/filming-protocol.md && [ "$(wc -l < docs/filming-protocol.md)" -ge 50 ] && [ "$(wc -l < docs/filming-protocol.md)" -le 120 ] && grep -F 'BB height' docs/filming-protocol.md && grep -Fi '60fps' docs/filming-protocol.md && grep -F 'CFR' docs/filming-protocol.md && grep -F 'tripod' docs/filming-protocol.md && grep -F 'fiducial' docs/filming-protocol.md && grep -F '5000' docs/filming-protocol.md && grep -F 'ffprobe' docs/filming-protocol.md && grep -F 'TDC' docs/filming-protocol.md && grep -F 'BDC' docs/filming-protocol.md && grep -c '\[ \]' docs/filming-protocol.md | awk '$1>=6 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/filming-protocol.md` exists.
    - Line count between 50 and 120 (concise one-pager).
    - Contains all four hard locks verbatim: BB height, fiducial, 60fps, CFR, tripod (single grep each).
    - Contains the ASCII diagram (a fenced code block with the labels `BB`, `tripod`, `wall`, `fiducial`).
    - Contains a 6-item checklist (six `[ ]` checkbox markers — exactly 6).
    - Contains the lighting paragraph mentioning `5000 K` (or `5000K`).
    - Contains the post-recording verification block referencing `ffprobe`.
    - Contains references to TDC and BDC (pedal-stroke terminology that downstream stroke-detection will use).
    - Does NOT include bad-example frames (D-17 — deferred to Phase 1).
  </acceptance_criteria>
  <done>
    The operator (the user) can read the file in 60 seconds, run through the 6-item checklist before pressing record, and produce a fixture ride that downstream pose work won't have to apologise for.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documented protocol → operator behavior | Document is advisory; operator can ignore it. Mitigation = clarity + the ffprobe gate in section 6 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-14 | Tampering | Filming protocol drift between document and downstream code | mitigate | Document captured in `docs/filming-protocol.md` referenced from later phases (Phase 1 ingestion checks 60fps + CFR via `ffprobe` per ING-03); divergence is detectable by code |
| T-00-15 | Information disclosure | Filming protocol in public repo discloses no PII | accept | No personal data in the doc; portfolio-grade content |

</threat_model>

<verification>
- File exists at the documented path
- All four hard locks present
- ASCII diagram is a fenced code block with the required labels
- 6-item checklist with checkbox syntax
- Lighting + post-recording verification + references sections present
- Line count is in the 50–120 range (concise one-pager)
</verification>

<success_criteria>
BOOT-05 met. ROADMAP Phase 0 success criterion #4 satisfied: `docs/filming-protocol.md` exists and specifies camera height = BB height, fiducial in frame, 60fps, CFR, framing, tripod-only.
</success_criteria>

<output>
Create `.planning/phases/00-bootstrap-cost-guardrails/00-05-SUMMARY.md` when done. Record the final line count and any wording decisions where Claude's discretion was used (e.g., specific phrasing of the 6-item checklist).
</output>
