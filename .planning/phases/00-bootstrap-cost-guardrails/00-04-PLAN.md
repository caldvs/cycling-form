---
phase: 00-bootstrap-cost-guardrails
plan: 04
type: execute
wave: 2
depends_on:
  - 00-03
files_modified:
  - scripts/bootstrap-gcp.env.example
  - scripts/bootstrap-gcp.sh
  - scripts/test-kill-switch.sh
autonomous: true
requirements:
  - BOOT-02
  - BOOT-03
  - BOOT-04
tags:
  - infra
  - gcp
  - kill-switch
  - bootstrap
  - bash

must_haves:
  truths:
    - "`scripts/bootstrap-gcp.env.example` documents every env var the bootstrap script reads, with safe default values where applicable"
    - "`scripts/bootstrap-gcp.sh` is an idempotent gcloud setup script that creates the GCP project (if missing), pins it to a single region, enables the required APIs, creates a $20/month budget with email alerts at 50/90/100%, deploys the Cloud Function from `infra/kill-switch/`, and wires the Pub/Sub topic to the budget notification channel"
    - "`scripts/test-kill-switch.sh` simulates a budget-exceeded Pub/Sub message, waits for the Cloud Function to process it, and verifies billing was disabled — designed to run against a THROWAWAY project per D-06"
    - "Both scripts are valid bash (pass `bash -n` syntax check), executable, and have shebangs"
    - "Both scripts use a single `$GCP_REGION` variable from the env file as the source of truth for the region (per D-09)"
    - "`bootstrap-gcp.sh` uses idempotent patterns (`gcloud ... --quiet || true` after existence-checks) per D-02 so re-running produces the same final state"
  artifacts:
    - path: "scripts/bootstrap-gcp.env.example"
      provides: "Env template — GCP_PROJECT_ID, GCP_REGION, BUDGET_USD, ALERT_EMAIL, BILLING_ACCOUNT_ID, KILL_SWITCH_PUBSUB_TOPIC, KILL_SWITCH_FUNCTION_NAME"
      contains: "GCP_REGION=us-central1"
    - path: "scripts/bootstrap-gcp.sh"
      provides: "Idempotent gcloud bootstrap script for the cost-bounded GCP project"
      exports: ["#!/usr/bin/env bash", "set -euo pipefail"]
    - path: "scripts/test-kill-switch.sh"
      provides: "End-to-end kill-switch test (simulates budget-exceeded message; verifies billing disabled)"
      exports: ["#!/usr/bin/env bash", "set -euo pipefail"]
  key_links:
    - from: "scripts/bootstrap-gcp.sh"
      to: "infra/kill-switch/"
      via: "`gcloud functions deploy --source=infra/kill-switch/`"
      pattern: "gcloud functions deploy.*--source=.*infra/kill-switch"
    - from: "scripts/bootstrap-gcp.sh"
      to: "scripts/bootstrap-gcp.env"
      via: "sourced env file"
      pattern: "source .*bootstrap-gcp.env"
    - from: "scripts/test-kill-switch.sh"
      to: "Pub/Sub topic + billing API"
      via: "`gcloud pubsub topics publish` + `gcloud billing projects describe`"
      pattern: "gcloud pubsub topics publish|gcloud billing projects describe"
---

<objective>
Author two operator-runnable scripts that, together, satisfy BOOT-02 (GCP project with budget+alerts), BOOT-03 (kill switch deployed AND tested), and BOOT-04 (single region pinned). The scripts are written here; the running of them against live GCP is plan 07's operator-only work.

Purpose: Encapsulate every GCP setup action in idempotent shell scripts so the user can run `./scripts/bootstrap-gcp.sh` once, recover from re-runs cleanly, and validate the kill switch fires correctly via `./scripts/test-kill-switch.sh`. Per D-01..D-03 (gcloud script, NOT Terraform), this is the documented Google-recommended quickstart pattern compressed into one reproducible artifact.

Output: Two shell scripts in `scripts/` plus an env-template documenting all configuration. No live GCP resources are created in this plan.
</objective>

<execution_context>
@/Users/callum/software-local/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callum/software-local/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/callum/software-local/vision/.planning/STATE.md
@/Users/callum/software-local/vision/.planning/ROADMAP.md
@/Users/callum/software-local/vision/.planning/REQUIREMENTS.md
@/Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md
@/Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-03-SUMMARY.md
@/Users/callum/software-local/vision/infra/kill-switch/main.py
@/Users/callum/software-local/vision/infra/kill-switch/README.md
@/Users/callum/software-local/vision/NOTICE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/bootstrap-gcp.env.example with all configuration variables</name>
  <files>scripts/bootstrap-gcp.env.example</files>
  <read_first>
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-03 (env template), §D-07 (us-central1 default), §D-08 (BQ US multi-region), §D-10 (region override note)
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-03-SUMMARY.md (kill-switch entry-point name, runtime)
  </read_first>
  <action>
    Create `scripts/bootstrap-gcp.env.example` with the following content. Use `KEY=value` lines, with a leading comment block at the top of the file explaining what each variable controls. Required variables (per D-03 + plan needs):

    - `GCP_PROJECT_ID=` — the user fills in (e.g., `vision-prod-2026`). No default.
    - `GCP_REGION=us-central1` — per D-07. Include this comment immediately above: `# If you are based in Europe/UK and add live demo links to your portfolio, switch to europe-west1 BEFORE running this script. Region cannot be changed in place after services are created. (Phase 0 D-10.)`
    - `GCP_BQ_LOCATION=US` — per D-08, BigQuery dataset multi-region. Comment: `# Multi-region location for BigQuery dataset. Aligns pricing with us-central1; switch to EU if GCP_REGION is europe-west1.`
    - `BUDGET_USD=20` — per BOOT-02 / ROADMAP success criterion #2.
    - `BUDGET_DISPLAY_NAME=vision-monthly-budget` — used by `gcloud billing budgets create`.
    - `ALERT_EMAIL=` — the user fills in (e.g., `dvscllm@gmail.com`). No default. Comment: `# Email that receives 50/90/100% budget alerts. Must be a valid Google account or have a Pub/Sub fallback wired separately.`
    - `BILLING_ACCOUNT_ID=` — the user fills in (e.g., `01ABCD-EFGH12-IJ34KL`). Comment: `# Run 'gcloud billing accounts list' to find this. Format: XXXXXX-XXXXXX-XXXXXX.`
    - `KILL_SWITCH_PUBSUB_TOPIC=vision-billing-alerts` — the topic the budget posts to AND the Cloud Function subscribes to.
    - `KILL_SWITCH_FUNCTION_NAME=stop-billing` — the deployed Cloud Function name (use the entry-point name from the kill-switch SUMMARY; default to `stop-billing` if unspecified there).
    - `KILL_SWITCH_RUNTIME=python312` — Cloud Functions Gen 2 runtime.
    - `KILL_SWITCH_REGION=$GCP_REGION` — kill switch deployed to the same region (D-09).
    - `KILL_SWITCH_SERVICE_ACCOUNT=vision-killswitch-sa` — short name (the full SA email is constructed in the script as `${KILL_SWITCH_SERVICE_ACCOUNT}@${GCP_PROJECT_ID}.iam.gserviceaccount.com`).

    End the file with this footer comment block:
    ```
    # ----------------------------------------------------------------------
    # USAGE:
    #   1. cp scripts/bootstrap-gcp.env.example scripts/bootstrap-gcp.env
    #   2. Fill in GCP_PROJECT_ID, ALERT_EMAIL, BILLING_ACCOUNT_ID.
    #   3. (Optional) Override GCP_REGION before any service exists.
    #   4. ./scripts/bootstrap-gcp.sh
    # ----------------------------------------------------------------------
    ```

    Verify with `bash -n scripts/bootstrap-gcp.env.example || true` (env files aren't bash scripts but should be parseable as `KEY=value` lines — instead, lint-check by `grep -E '^[A-Z_]+=.*$|^#|^$' scripts/bootstrap-gcp.env.example | wc -l` equalling the line count of the file).
  </action>
  <verify>
    <automated>cd /Users/callum/software-local/vision && test -f scripts/bootstrap-gcp.env.example && grep -F 'GCP_REGION=us-central1' scripts/bootstrap-gcp.env.example && grep -F 'BUDGET_USD=20' scripts/bootstrap-gcp.env.example && grep -F 'KILL_SWITCH_PUBSUB_TOPIC=' scripts/bootstrap-gcp.env.example && grep -F 'KILL_SWITCH_RUNTIME=python312' scripts/bootstrap-gcp.env.example && grep -F 'europe-west1' scripts/bootstrap-gcp.env.example && grep -F 'GCP_BQ_LOCATION=US' scripts/bootstrap-gcp.env.example</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `scripts/bootstrap-gcp.env.example`.
    - Contains `GCP_REGION=us-central1` verbatim (D-07).
    - Contains `BUDGET_USD=20` (BOOT-02).
    - Contains `GCP_BQ_LOCATION=US` (D-08).
    - Contains the `europe-west1` override comment (D-10).
    - Documents `KILL_SWITCH_PUBSUB_TOPIC`, `KILL_SWITCH_FUNCTION_NAME`, `KILL_SWITCH_RUNTIME`, `KILL_SWITCH_REGION`, `KILL_SWITCH_SERVICE_ACCOUNT`.
    - Empty user-fill-in fields (no default values) for `GCP_PROJECT_ID`, `ALERT_EMAIL`, `BILLING_ACCOUNT_ID`.
    - Footer USAGE block instructs `cp` to `bootstrap-gcp.env`.
  </acceptance_criteria>
  <done>
    A future operator can `cp scripts/bootstrap-gcp.env.example scripts/bootstrap-gcp.env`, fill in three blank fields, and have a complete bootstrap config.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/bootstrap-gcp.sh (idempotent gcloud setup)</name>
  <files>scripts/bootstrap-gcp.sh</files>
  <read_first>
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-01..D-10 (script-based provisioning, idempotent, single-region pin)
    - /Users/callum/software-local/vision/infra/kill-switch/README.md (deployment expectations)
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-03-SUMMARY.md (function entry-point name, runtime)
    - /Users/callum/software-local/vision/scripts/bootstrap-gcp.env.example (variable names)
  </read_first>
  <action>
    Create `scripts/bootstrap-gcp.sh` as a bash script with the following structure. Make it executable (`chmod +x scripts/bootstrap-gcp.sh`).

    Top of file:
    ```
    #!/usr/bin/env bash
    # bootstrap-gcp.sh — idempotent GCP setup for the Vision project.
    # Satisfies BOOT-02 (budget + alerts), BOOT-03 (kill switch deployed),
    # BOOT-04 (single region pin). Operator runs once after editing
    # scripts/bootstrap-gcp.env. Re-running is safe (idempotent per D-02).
    set -euo pipefail
    ```

    Section 1 — Load env:
    - Resolve script directory: `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`.
    - Define ENV_FILE: `ENV_FILE="${SCRIPT_DIR}/bootstrap-gcp.env"`.
    - Fail early with a clear message if the env file is missing: `[[ -f "$ENV_FILE" ]] || { echo "ERROR: $ENV_FILE not found. Copy bootstrap-gcp.env.example and fill it in."; exit 1; }`.
    - Source it: `set -a; source "$ENV_FILE"; set +a`.
    - Validate required fields: for each of `GCP_PROJECT_ID`, `GCP_REGION`, `GCP_BQ_LOCATION`, `BUDGET_USD`, `ALERT_EMAIL`, `BILLING_ACCOUNT_ID`, `KILL_SWITCH_PUBSUB_TOPIC`, `KILL_SWITCH_FUNCTION_NAME`, `KILL_SWITCH_RUNTIME`, `KILL_SWITCH_REGION`, `KILL_SWITCH_SERVICE_ACCOUNT`, fail with a clear error if the variable is empty.

    Section 2 — Pre-flight checks:
    - Check that `gcloud` is installed: `command -v gcloud >/dev/null || { echo "ERROR: gcloud not on PATH"; exit 1; }`.
    - Check the user is authenticated: `gcloud auth print-identity-token >/dev/null 2>&1 || { echo "ERROR: not authenticated. Run 'gcloud auth login' and 'gcloud auth application-default login' first."; exit 1; }`. (Per D-11, ADC is the auth path.)
    - Print a summary of what will be created/configured and ask for confirmation: `echo "About to bootstrap GCP project '$GCP_PROJECT_ID' in region '$GCP_REGION' with \$$BUDGET_USD/month budget. Continue? [y/N]"; read -r REPLY; [[ "$REPLY" =~ ^[Yy]$ ]] || { echo "Aborted."; exit 1; }`.

    Section 3 — Create or select project (idempotent):
    - `if gcloud projects describe "$GCP_PROJECT_ID" >/dev/null 2>&1; then echo "Project $GCP_PROJECT_ID already exists, skipping create."; else gcloud projects create "$GCP_PROJECT_ID" --name="Vision (Cycling Form Analyzer)"; fi`.
    - Link billing: `gcloud billing projects link "$GCP_PROJECT_ID" --billing-account="$BILLING_ACCOUNT_ID"`.
    - Set the active project: `gcloud config set project "$GCP_PROJECT_ID"`.

    Section 4 — Enable required APIs (idempotent — `services enable` is a no-op if already enabled):
    - Enable in one batch call: `gcloud services enable cloudbilling.googleapis.com cloudbilling-budget.googleapis.com billingbudgets.googleapis.com pubsub.googleapis.com cloudfunctions.googleapis.com run.googleapis.com cloudbuild.googleapis.com artifactregistry.googleapis.com eventarc.googleapis.com logging.googleapis.com iam.googleapis.com --project="$GCP_PROJECT_ID"`.
    - NOTE: include comment `# We enable billingbudgets early so 'gcloud billing budgets' works. cloudbilling-budget.googleapis.com may not exist as a separate service in all gcloud versions — both names listed for forward compatibility.`

    Section 5 — Create Pub/Sub topic (idempotent):
    - `if ! gcloud pubsub topics describe "$KILL_SWITCH_PUBSUB_TOPIC" --project="$GCP_PROJECT_ID" >/dev/null 2>&1; then gcloud pubsub topics create "$KILL_SWITCH_PUBSUB_TOPIC" --project="$GCP_PROJECT_ID"; else echo "Topic $KILL_SWITCH_PUBSUB_TOPIC already exists, skipping."; fi`.

    Section 6 — Create kill-switch service account + grant least-privilege IAM (idempotent):
    - `SA_EMAIL="${KILL_SWITCH_SERVICE_ACCOUNT}@${GCP_PROJECT_ID}.iam.gserviceaccount.com"`.
    - `if ! gcloud iam service-accounts describe "$SA_EMAIL" --project="$GCP_PROJECT_ID" >/dev/null 2>&1; then gcloud iam service-accounts create "$KILL_SWITCH_SERVICE_ACCOUNT" --display-name="Vision billing kill-switch" --project="$GCP_PROJECT_ID"; fi`.
    - Grant `roles/billing.projectManager` on the project (this is the role that permits `updateBillingInfo`): `gcloud projects add-iam-policy-binding "$GCP_PROJECT_ID" --member="serviceAccount:$SA_EMAIL" --role="roles/billing.projectManager" --condition=None`. (`add-iam-policy-binding` is naturally idempotent.)

    Section 7 — Deploy the Cloud Function (Gen 2) from `infra/kill-switch/`:
    - `REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"`.
    - `gcloud functions deploy "$KILL_SWITCH_FUNCTION_NAME" --gen2 --project="$GCP_PROJECT_ID" --region="$KILL_SWITCH_REGION" --runtime="$KILL_SWITCH_RUNTIME" --source="$REPO_ROOT/infra/kill-switch" --entry-point="$KILL_SWITCH_FUNCTION_NAME" --trigger-topic="$KILL_SWITCH_PUBSUB_TOPIC" --service-account="$SA_EMAIL" --no-allow-unauthenticated --max-instances=1`. (Comment: `# --max-instances=1 caps blast radius if the function is invoked repeatedly.`)
    - Add a NOTE comment that `--entry-point` MUST match the function name inside `infra/kill-switch/main.py` (per plan-03 SUMMARY; default `stop_billing`); the script trusts the env var value.

    Section 8 — Create the budget with Pub/Sub notification (idempotent via list-and-conditional-create):
    - Look up the budget by display name: `EXISTING_BUDGET=$(gcloud billing budgets list --billing-account="$BILLING_ACCOUNT_ID" --filter="displayName=$BUDGET_DISPLAY_NAME" --format="value(name)" 2>/dev/null || true)`.
    - If empty, create:
      ```
      gcloud billing budgets create \
        --billing-account="$BILLING_ACCOUNT_ID" \
        --display-name="$BUDGET_DISPLAY_NAME" \
        --budget-amount="${BUDGET_USD}USD" \
        --filter-projects="projects/$(gcloud projects describe "$GCP_PROJECT_ID" --format='value(projectNumber)')" \
        --threshold-rule=percent=0.5 \
        --threshold-rule=percent=0.9 \
        --threshold-rule=percent=1.0 \
        --notifications-rule-pubsub-topic="projects/$GCP_PROJECT_ID/topics/$KILL_SWITCH_PUBSUB_TOPIC" \
        --notifications-rule-monitoring-notification-channels="$(gcloud beta monitoring channels list --project="$GCP_PROJECT_ID" --filter="type=email AND labels.email_address=$ALERT_EMAIL" --format='value(name)' | head -n1)"
      ```
      with a comment that if the email channel doesn't exist yet, the script must create it first (handle this — see step below).
    - Before the create, ensure an email Monitoring notification channel exists for `$ALERT_EMAIL`:
      ```
      EMAIL_CHANNEL=$(gcloud beta monitoring channels list --project="$GCP_PROJECT_ID" --filter="type=email AND labels.email_address=$ALERT_EMAIL" --format='value(name)' 2>/dev/null | head -n1)
      if [[ -z "$EMAIL_CHANNEL" ]]; then
        EMAIL_CHANNEL=$(gcloud beta monitoring channels create --project="$GCP_PROJECT_ID" --type=email --display-name="Vision budget alerts" --channel-labels="email_address=$ALERT_EMAIL" --format='value(name)')
      fi
      ```
    - If `EXISTING_BUDGET` is non-empty, print: `echo "Budget '$BUDGET_DISPLAY_NAME' already exists at $EXISTING_BUDGET — skipping create. To update, delete and re-run."`. (Idempotent.)

    Section 9 — Set the default compute region (so subsequent `gcloud run jobs deploy` calls inherit it; D-09):
    - `gcloud config set compute/region "$GCP_REGION" --project="$GCP_PROJECT_ID"`.
    - `gcloud config set run/region "$GCP_REGION" --project="$GCP_PROJECT_ID"`.

    Section 10 — Print a success summary at the end:
    - `echo "GCP bootstrap complete."`
    - Print: project id, region, budget, alert email, kill-switch function URL/name, Pub/Sub topic, the service account email.
    - Print the recommended next step: `echo "Next: run ./scripts/test-kill-switch.sh against a THROWAWAY project to verify the kill switch fires before relying on it on this project. See plan 07."`.

    The script MUST pass `bash -n scripts/bootstrap-gcp.sh` (syntax-only check). It will NOT be runnable in this plan (no live GCP), but should be syntactically valid and logically reviewable.

    Make the script executable: `chmod +x scripts/bootstrap-gcp.sh`.
  </action>
  <verify>
    <automated>cd /Users/callum/software-local/vision && test -x scripts/bootstrap-gcp.sh && bash -n scripts/bootstrap-gcp.sh && head -n1 scripts/bootstrap-gcp.sh | grep -F '#!/usr/bin/env bash' && grep -F 'set -euo pipefail' scripts/bootstrap-gcp.sh && grep -F 'gcloud functions deploy' scripts/bootstrap-gcp.sh && grep -E 'gcloud functions deploy.*--source=' scripts/bootstrap-gcp.sh && grep -F 'infra/kill-switch' scripts/bootstrap-gcp.sh && grep -F 'gcloud pubsub topics create' scripts/bootstrap-gcp.sh && grep -F 'gcloud billing budgets create' scripts/bootstrap-gcp.sh && grep -F 'percent=0.5' scripts/bootstrap-gcp.sh && grep -F 'percent=0.9' scripts/bootstrap-gcp.sh && grep -F 'percent=1.0' scripts/bootstrap-gcp.sh && grep -F 'roles/billing.projectManager' scripts/bootstrap-gcp.sh && grep -F 'gcloud iam service-accounts create' scripts/bootstrap-gcp.sh && grep -F 'gcloud projects describe' scripts/bootstrap-gcp.sh && grep -E 'if.*gcloud.*describe' scripts/bootstrap-gcp.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/bootstrap-gcp.sh` exists, is executable, and starts with `#!/usr/bin/env bash`.
    - Passes `bash -n` syntax check.
    - Contains `set -euo pipefail`.
    - Sources `scripts/bootstrap-gcp.env` (not the example file).
    - Uses idempotent patterns (existence checks before create) for project, Pub/Sub topic, service account, and budget — verified by `grep -E 'if.*gcloud.*describe' scripts/bootstrap-gcp.sh | wc -l >= 3`.
    - Calls `gcloud functions deploy ... --source=... infra/kill-switch ...` referencing the vendored kill switch.
    - Creates the budget with `--threshold-rule=percent=0.5`, `=0.9`, `=1.0` (BOOT-02 + ROADMAP success criterion #2).
    - Grants `roles/billing.projectManager` to the kill-switch service account (least-privilege IAM for `updateBillingInfo`).
    - Sets `gcloud config set compute/region` and `gcloud config set run/region` to `$GCP_REGION` (BOOT-04 / D-09).
    - Prints a success summary at the end with the recommended next step (run the kill-switch test).
  </acceptance_criteria>
  <done>
    The operator can run `./scripts/bootstrap-gcp.sh` with a populated env file and have a complete cost-bounded GCP project plus a deployed kill-switch Cloud Function. Re-running the script is safe (no duplicate-resource errors).
  </done>
</task>

<task type="auto">
  <name>Task 3: Write scripts/test-kill-switch.sh (end-to-end kill-switch verification)</name>
  <files>scripts/test-kill-switch.sh</files>
  <read_first>
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-06 (kill switch must be tested end-to-end on a throwaway project before Phase 0 closes)
    - /Users/callum/software-local/vision/infra/kill-switch/main.py (Pub/Sub message shape — message must include `budgetAmount` and `costAmount`)
    - /Users/callum/software-local/vision/scripts/bootstrap-gcp.env.example (same env vars)
  </read_first>
  <action>
    Create `scripts/test-kill-switch.sh` (chmod +x). Structure:

    Top:
    ```
    #!/usr/bin/env bash
    # test-kill-switch.sh — end-to-end test of the billing kill switch.
    #
    # ⚠️ THIS SCRIPT DISABLES BILLING ON THE TARGET PROJECT.
    # ⚠️ RUN ONLY AGAINST A DISPOSABLE THROWAWAY PROJECT (D-06).
    #
    # Procedure:
    #   1. Confirm the operator understands the warning (interactive y/N).
    #   2. Publish a synthetic budget-exceeded message to KILL_SWITCH_PUBSUB_TOPIC.
    #   3. Poll Cloud Function logs for evidence the function ran.
    #   4. Poll billing state via 'gcloud billing projects describe' until billingEnabled=False.
    #   5. Print PASS/FAIL.
    set -euo pipefail
    ```

    Section 1 — Load env (same pattern as `bootstrap-gcp.sh`):
    - Source `scripts/bootstrap-gcp.env`; validate required fields (`GCP_PROJECT_ID`, `KILL_SWITCH_PUBSUB_TOPIC`, `KILL_SWITCH_FUNCTION_NAME`, `KILL_SWITCH_REGION`).

    Section 2 — Loud confirmation gate (D-06 + the kill-switch README's one-way-door warning):
    - Print a 4-line warning block in ALL CAPS that this WILL disable billing on `$GCP_PROJECT_ID` and that the operator must confirm.
    - Require typing the literal string `DISABLE BILLING ON $GCP_PROJECT_ID` (with the project id substituted) before proceeding. Anything else aborts with exit 2.

    Section 3 — Capture the pre-test state:
    - `PRE_STATE=$(gcloud billing projects describe "$GCP_PROJECT_ID" --format='value(billingEnabled)')`.
    - If `PRE_STATE` is not `True`, print "Billing is already disabled on $GCP_PROJECT_ID — cannot run a meaningful test." and exit 3.

    Section 4 — Publish the synthetic budget-exceeded message:
    - Build the message body (this MUST match the GCP Billing budget notification shape; reference: https://cloud.google.com/billing/docs/how-to/notify):
      ```
      MESSAGE_BODY=$(cat <<EOF
      {
        "budgetDisplayName": "vision-test-budget",
        "alertThresholdExceeded": 1.0,
        "costAmount": 25.00,
        "costIntervalStart": "$(date -u -v-1d '+%Y-%m-%dT00:00:00Z' 2>/dev/null || date -u -d 'yesterday' '+%Y-%m-%dT00:00:00Z')",
        "budgetAmount": 20.00,
        "budgetAmountType": "SPECIFIED_AMOUNT",
        "currencyCode": "USD"
      }
      EOF
      )
      ```
      Note that GCP Billing also sends a `projectId` field separately as an attribute, not in the body — our adapted `main.py` (from plan 03) reads the project id from either source. The attribute is `billingAccountId`; we publish without a custom attribute since the adapted function reads from the body. (If plan-03 adaptation expects an attribute, capture that in plan-03 SUMMARY and add `--attribute="projectId=$GCP_PROJECT_ID"` here — author the script to include the attribute defensively.)
    - Publish: `gcloud pubsub topics publish "$KILL_SWITCH_PUBSUB_TOPIC" --project="$GCP_PROJECT_ID" --message="$MESSAGE_BODY" --attribute="projectId=$GCP_PROJECT_ID,budgetId=test-budget"`.

    Section 5 — Poll Cloud Function logs for evidence the function ran (timeout 120s):
    - ```
      START_TS=$(date -u '+%Y-%m-%dT%H:%M:%SZ')
      DEADLINE=$(($(date +%s) + 120))
      while [[ $(date +%s) -lt $DEADLINE ]]; do
        if gcloud functions logs read "$KILL_SWITCH_FUNCTION_NAME" --gen2 --region="$KILL_SWITCH_REGION" --project="$GCP_PROJECT_ID" --limit=20 --format='value(log)' --start-time="$START_TS" 2>/dev/null | grep -qiE 'disabled billing|updateBillingInfo|stop_billing'; then
          echo "✓ Cloud Function logged the disable-billing action."
          break
        fi
        sleep 5
      done
      ```
    - If the loop times out without finding the log marker, fail with exit 4 and print the last 50 log lines for debugging.

    Section 6 — Poll billing state until disabled (timeout 180s, polling every 10s):
    - ```
      DEADLINE=$(($(date +%s) + 180))
      while [[ $(date +%s) -lt $DEADLINE ]]; do
        POST_STATE=$(gcloud billing projects describe "$GCP_PROJECT_ID" --format='value(billingEnabled)')
        if [[ "$POST_STATE" == "False" ]]; then
          echo "✓ Billing disabled on $GCP_PROJECT_ID."
          break
        fi
        sleep 10
      done
      ```
    - If the loop times out with `billingEnabled` still `True`, fail with exit 5 and print: "Kill switch did not disable billing within 180s. Check Cloud Function logs and IAM permissions."

    Section 7 — Final PASS/FAIL:
    - On success: print `KILL-SWITCH TEST: PASS` and document the recovery procedure:
      `echo "To recover: gcloud billing projects link $GCP_PROJECT_ID --billing-account=<BILLING_ACCOUNT_ID>"`.
    - On any failure, the `set -e` plus the explicit `exit` codes propagate.

    Make executable: `chmod +x scripts/test-kill-switch.sh`. Syntax-check: `bash -n scripts/test-kill-switch.sh`.
  </action>
  <verify>
    <automated>cd /Users/callum/software-local/vision && test -x scripts/test-kill-switch.sh && bash -n scripts/test-kill-switch.sh && head -n1 scripts/test-kill-switch.sh | grep -F '#!/usr/bin/env bash' && grep -F 'set -euo pipefail' scripts/test-kill-switch.sh && grep -F 'DISABLE BILLING ON' scripts/test-kill-switch.sh && grep -F 'gcloud pubsub topics publish' scripts/test-kill-switch.sh && grep -F 'budgetAmount' scripts/test-kill-switch.sh && grep -F 'costAmount' scripts/test-kill-switch.sh && grep -F 'gcloud functions logs read' scripts/test-kill-switch.sh && grep -F 'gcloud billing projects describe' scripts/test-kill-switch.sh && grep -F 'KILL-SWITCH TEST: PASS' scripts/test-kill-switch.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/test-kill-switch.sh` exists, is executable, starts with `#!/usr/bin/env bash`.
    - Passes `bash -n` syntax check.
    - Contains the loud DISABLE-BILLING warning + interactive confirmation gate (operator must type the literal phrase).
    - Publishes a Pub/Sub message containing `budgetAmount` and `costAmount` keys matching the GCP Billing notification JSON shape.
    - Polls Cloud Function logs via `gcloud functions logs read` for evidence of the disable-billing action (timeout 120s, 5s intervals).
    - Polls `gcloud billing projects describe` until `billingEnabled=False` (timeout 180s, 10s intervals).
    - Exits 0 on PASS with the line `KILL-SWITCH TEST: PASS`.
    - Exits non-zero on any failure with a clear diagnostic.
    - Documents the recovery command (`gcloud billing projects link`) in the success output.
  </acceptance_criteria>
  <done>
    The operator (plan 07) can run `./scripts/test-kill-switch.sh` against a throwaway project and obtain a clear PASS/FAIL signal in under ~5 minutes. ROADMAP success criterion #3 is satisfied by this script + plan 07's execution.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell script → gcloud CLI → GCP API | Script holds the operator's `gcloud auth` identity; mistakes execute against the operator's GCP organization |
| `scripts/bootstrap-gcp.env` (gitignored) → script runtime | The env file contains the billing account id and alert email; treated as a secret-shaped file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-10 | Tampering | `scripts/bootstrap-gcp.env` (with real billing account id) committed by mistake | mitigate | Plan 02's `.gitignore` excludes `.env.*` while keeping `.env.example` trackable via negation; `bootstrap-gcp.sh` fails fast if the file is missing rather than silently using defaults |
| T-00-11 | Denial of service | `test-kill-switch.sh` accidentally run against the live (non-throwaway) project | mitigate | Script requires the operator to type the literal phrase `DISABLE BILLING ON <PROJECT_ID>` to proceed; pre-test check fails if billing is already disabled; success message documents the recovery command. The script's name and top-of-file comment block are explicit warnings. |
| T-00-12 | Elevation of privilege | `roles/billing.projectManager` granted to the kill-switch SA can disable billing on any project the SA is granted on | mitigate | `bootstrap-gcp.sh` grants the role at the project level only (not at billing-account level); the SA has no other roles; `--max-instances=1` on the function caps blast radius |
| T-00-13 | Spoofing | Pub/Sub message published by anyone with `pubsub.publisher` on the topic can trigger the function | mitigate | `--no-allow-unauthenticated` on the function deploy; default IAM on the Pub/Sub topic restricts publish to project members; the topic is in the same project as the function so cross-project publishing is impossible without explicit IAM grants |
| T-00-SC | Tampering | gcloud version-pinning | accept | gcloud is operator-installed (not vendored); the script's pre-flight check requires gcloud on PATH but does not pin a version. Operator is solo; acceptable for v1. |

</threat_model>

<verification>
- `bash -n` passes on both scripts (syntactically valid bash)
- All three files exist; the two scripts are executable
- `bootstrap-gcp.sh` uses idempotent existence-check-then-create patterns (per D-02)
- `bootstrap-gcp.sh` references the vendored kill-switch path (`infra/kill-switch/`)
- Budget thresholds 50/90/100% are explicitly present in the script
- `test-kill-switch.sh` includes a loud confirmation gate, a synthetic Pub/Sub message with the correct shape, log polling, and billing-state polling
</verification>

<success_criteria>
BOOT-02 (script authors the budget+alerts), BOOT-03 (script deploys the kill switch), BOOT-04 (script pins region) are all addressed at the source-of-truth level. Plan 07 will run these scripts against live GCP to complete BOOT-02..04.
</success_criteria>

<output>
Create `.planning/phases/00-bootstrap-cost-guardrails/00-04-SUMMARY.md` when done. Document any edge cases discovered while writing the budget-creation block (gcloud version compatibility, especially around `gcloud billing budgets create` arg names, which have changed across gcloud versions). Note the exact gcloud-version-tested baseline.
</output>
