---
phase: 00-bootstrap-cost-guardrails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - CONTRIBUTING.md
  - NOTICE
autonomous: true
requirements:
  - BOOT-07
tags:
  - repo-hygiene
  - privacy
  - portfolio

must_haves:
  truths:
    - "`git status` shows zero secret-shaped files as untracked after a typical local-dev workflow (no `*.json` credentials at root, no `.env` files, no raw `.fit` files committed)"
    - "Contributors (including future Claude executors) see a one-line 'never commit secrets' note in `CONTRIBUTING.md`"
    - "Upstream credit for the vendored Cyclenerd kill switch is present in `NOTICE` (filled in by plan 03)"
  artifacts:
    - path: ".gitignore"
      provides: "Excludes credentials, env files, raw FIT (per BOOT-07 + D-14), large fixture artifacts, Python and tool caches, OS artifacts"
      contains: "*.fit"
    - path: "CONTRIBUTING.md"
      provides: "Dev quickstart + 'no secrets in commits' note per D-27"
      contains: "Never commit secrets"
    - path: "NOTICE"
      provides: "Stub for upstream credit (Cyclenerd kill switch — filled by plan 03)"
  key_links:
    - from: ".gitignore"
      to: "scripts/bootstrap-gcp.env (gitignored)"
      via: ".env exclusion glob"
      pattern: "\\.env"
    - from: "CONTRIBUTING.md"
      to: ".gitignore"
      via: "documented 'no secrets' policy backed by gitignore enforcement"
      pattern: "no secrets|never commit"
---

<objective>
Establish repo hygiene before any code lands: a comprehensive `.gitignore` that prevents credentials, env files, raw FIT files (which contain GPS — privacy hygiene per BOOT-07/D-14), and large fixture artifacts from ever being committed; a `CONTRIBUTING.md` skeleton with the load-bearing "never commit secrets" note (D-27); and an empty `NOTICE` stub that plan 03 will populate with Cyclenerd upstream credit.

Purpose: BOOT-07. Privacy and portfolio-credibility hygiene from commit #1 of a public repo (D-25). A leaked `.env` or a raw FIT with home GPS coordinates is a forever-mistake on a public GitHub repo.

Output: A repo that refuses to track secrets, FIT files, or `.env` files by default.
</objective>

<execution_context>
@/Users/callum/software-local/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callum/software-local/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/callum/software-local/vision/.planning/STATE.md
@/Users/callum/software-local/vision/.planning/ROADMAP.md
@/Users/callum/software-local/vision/.planning/REQUIREMENTS.md
@/Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .gitignore, CONTRIBUTING.md skeleton, NOTICE stub</name>
  <files>.gitignore, CONTRIBUTING.md, NOTICE</files>
  <read_first>
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-14 (gitignore content list) and §D-25..D-27 (public-repo policy)
    - /Users/callum/software-local/vision/.planning/REQUIREMENTS.md §BOOT-07 verbatim
    - quality-gate gitignore checklist in the planning context (covers `*.json`, `.env`, `.env.*`, `*.fit`, caches, etc.)
  </read_first>
  <action>
    Create `.gitignore` at the repo root. Group entries by category with a leading comment for each group; in each group, list one pattern per line. The file MUST include these patterns verbatim (exact strings — D-14 + quality-gate enforcement):

    Credentials & env (group header `# Credentials, env, secrets`):
    - `*.json` (CAVEAT: this is the root-only intent per D-14, but `.gitignore` at root applies recursively. We accept that downstream phases that need to commit JSON schemas — e.g. BigQuery schemas in Phase 3 — will explicitly add a negation like `!infra/bigquery/schemas/*.json` at that time. Phase 0 has no committed JSON, so the broad rule is correct here. Add a comment in the file: `# NOTE: blanket *.json — re-allow specific JSON via !path in later phases`)
    - `.env`
    - `.env.*`
    - `!.env.example` (a negation so D-03's `scripts/bootstrap-gcp.env.example` is still tracked — but actually D-03 named the file with `.env.example` suffix, which the `.env.*` pattern would exclude, so the negation is essential. Add a comment: `# Re-allow committed example env templates`)
    - `secrets/`
    - `*.pem`
    - `*.key` (private-key files)

    Sport-telemetry privacy (group header `# Raw telemetry files contain GPS — never commit (BOOT-07)`):
    - `*.fit`
    - `*.FIT` (case-sensitive filesystems)
    - `*.tcx`
    - `*.gpx`

    Large fixture artifacts (group header `# Large media / derived artifacts — not for git`):
    - `*.mp4`
    - `*.mov`
    - `*.avi`
    - `*.parquet`

    Python (group header `# Python`):
    - `__pycache__/`
    - `*.py[cod]`
    - `*.egg-info/`
    - `*.egg`
    - `build/`
    - `dist/`
    - `.venv/`
    - `venv/`

    Tool caches (group header `# Tool caches`):
    - `.uv-cache/`
    - `.mypy_cache/`
    - `.ruff_cache/`
    - `.pytest_cache/`
    - `.coverage`
    - `htmlcov/`
    - `node_modules/` (in case any tooling needs npm later)

    OS / editor (group header `# OS / editor`):
    - `.DS_Store`
    - `Thumbs.db`
    - `.idea/`
    - `.vscode/`
    - `*.swp`

    Create `CONTRIBUTING.md` with this structure (concise, one-pager):
    - `# Contributing to Vision` (H1)
    - One paragraph: "Vision is a solo-developer portfolio project. External contributions aren't expected, but the repo is public so others can read it. This file documents how the project is built locally."
    - `## Local development quickstart` (H2) — three bash blocks:
      1. Install uv: `curl -LsSf https://astral.sh/uv/install.sh | sh` (Linux/macOS)
      2. Sync env: `uv sync`
      3. Run gates: `uv run ruff check . && uv run mypy lib tests && uv run pytest -q`
    - `## Repo conventions` (H2) — three bullets:
      - "Python 3.12 only (MediaPipe has no 3.13 wheels as of 2026-05)."
      - "All linting, type-checking, and tests go through `uv run`. Don't use the system Python."
      - "Filename and code conventions are tracked in `CLAUDE.md` and inherited from `.planning/research/STACK.md`."
    - `## Never commit secrets` (H2 — load-bearing per D-27) — bullet list:
      - "Service-account JSON files: never. ADC via `gcloud auth application-default login` is the only auth path in v1 (D-11)."
      - "`.env` files: never. Use `scripts/bootstrap-gcp.env.example` as the template; copy to `scripts/bootstrap-gcp.env` locally (gitignored)."
      - "Raw `.fit`, `.tcx`, `.gpx` ride files: never. They often contain GPS home coordinates."
      - "If you've accidentally committed a secret: rotate the credential immediately, then use `git filter-repo` or BFG to scrub history. Force-push only after coordinating."
    - `## Bug reports and issues` (H2) — one line: "Open a GitHub issue; this is a portfolio project, response time is best-effort."

    Create `NOTICE` as a minimal stub — plan 03 will replace its contents with the Cyclenerd upstream credit. For now, write:
    ```
    Vision — Cycling Form & Performance Analyzer
    Copyright (c) 2026 Callum

    This project vendors upstream open-source code. See the per-component sections
    below for credit and licenses.

    -----------------------------------------------------------------------------
    (vendored components will be listed here as they are added — see plan 00-03)
    -----------------------------------------------------------------------------
    ```

    Test the `.gitignore` works as intended:
    - `touch .env && git status --porcelain | grep -F '.env' && echo "BUG: .env is tracked"` — should NOT print BUG.
    - `touch foo.fit && git status --porcelain | grep -F 'foo.fit' && echo "BUG: foo.fit is tracked"` — should NOT print BUG.
    - `touch scripts/bootstrap-gcp.env.example && git check-ignore scripts/bootstrap-gcp.env.example; echo "exit=$?"` — should print `exit=1` (not ignored, because of the `!.env.example` negation).
    - Clean up test files (`rm -f .env foo.fit scripts/bootstrap-gcp.env.example` and `rmdir scripts 2>/dev/null || true`) — do NOT leave test artifacts behind.
  </action>
  <verify>
    <automated>cd /Users/callum/software-local/vision && test -f .gitignore && test -f CONTRIBUTING.md && test -f NOTICE && grep -F '*.fit' .gitignore && grep -F '*.json' .gitignore && grep -F '.env' .gitignore && grep -F 'secrets/' .gitignore && grep -F '__pycache__/' .gitignore && grep -F '.venv/' .gitignore && grep -F '.mypy_cache/' .gitignore && grep -F '.ruff_cache/' .gitignore && grep -F '.pytest_cache/' .gitignore && grep -F 'node_modules/' .gitignore && grep -F '.DS_Store' .gitignore && grep -Fi 'never commit secrets' CONTRIBUTING.md && grep -F 'gcloud auth application-default login' CONTRIBUTING.md && grep -F 'Vision' NOTICE && touch .env && git check-ignore .env && rm .env && touch foo.fit && git check-ignore foo.fit && rm foo.fit</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` exists at repo root.
    - `.gitignore` matches all 30+ patterns from D-14 + quality-gate (single grep per pattern; each `>= 1` match — the verify command above checks the high-leverage ones).
    - `.gitignore` includes the `!.env.example` negation so the bootstrap env template stays trackable.
    - `git check-ignore .env` exits 0 (file would be ignored) for a touched `.env`.
    - `git check-ignore foo.fit` exits 0 for a touched `foo.fit`.
    - `CONTRIBUTING.md` exists with H2 sections `Local development quickstart`, `Repo conventions`, `Never commit secrets`, `Bug reports and issues`.
    - `CONTRIBUTING.md` mentions `gcloud auth application-default login` (ADC is the only auth path, per D-11).
    - `CONTRIBUTING.md` contains the literal phrase "Never commit secrets" (case-insensitive grep `>= 1`).
    - `NOTICE` exists, references `Vision`, and contains a placeholder for vendored components (plan 03 fills it in).
  </acceptance_criteria>
  <done>
    A `git status` after a normal dev session never shows credentials/env/FIT/cache files as untracked; a public push cannot leak `.env`, `.fit`, or `*.pem` files; the contributor doc points to the right env-template path and the right auth method.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local working tree → public GitHub | A commit is irreversible on a public repo without history rewrite + credential rotation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03 | Information disclosure | Local `.env` with GCP project id, alert email, budget config | mitigate | `.gitignore` excludes `.env` and `.env.*`; `!.env.example` negation keeps the template visible while the real env is gitignored |
| T-00-04 | Information disclosure | Raw `.fit` ride file (contains GPS home coordinates for outdoor rides) | mitigate | `.gitignore` excludes `*.fit`, `*.FIT`, `*.tcx`, `*.gpx`; `CONTRIBUTING.md` documents the privacy rationale |
| T-00-05 | Information disclosure | Service-account JSON files | mitigate | `.gitignore` excludes `*.json` at root with a documented negation pattern for committed schemas in later phases; `CONTRIBUTING.md` documents ADC as the only Phase-0 auth path (D-11) |
| T-00-06 | Repudiation | Future contributor accidentally commits a secret | accept | Solo project; if a secret leaks, the procedure is rotate-and-scrub documented in `CONTRIBUTING.md` |

</threat_model>

<verification>
- `.gitignore` blocks the canonical secret-shaped files: `.env`, `*.fit`, `secrets/`, `*.pem`, `*.key` (test via `git check-ignore`)
- `.gitignore` allows `.env.example` files (test via the negation pattern)
- `CONTRIBUTING.md` documents the never-commit-secrets policy with concrete commands
- `NOTICE` exists as a stub (plan 03 will populate Cyclenerd credit)
</verification>

<success_criteria>
BOOT-07 met: `.gitignore` excludes credentials, env files, and FIT files containing GPS. Contributor doc documents the policy. Public-repo (D-25) safety net is in place from commit #1.
</success_criteria>

<output>
Create `.planning/phases/00-bootstrap-cost-guardrails/00-02-SUMMARY.md` when done. Note any gitignore patterns added beyond the quality-gate list (e.g., editor swap files) so later phases know what is already covered.
</output>
