---
phase: 00-bootstrap-cost-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - lib/vision/__init__.py
  - tests/__init__.py
  - tests/test_imports.py
autonomous: true
requirements:
  - BOOT-01
tags:
  - python
  - toolchain
  - uv
  - ruff
  - mypy
  - pytest

must_haves:
  truths:
    - "`uv sync` reproduces the dev environment from pyproject.toml + uv.lock"
    - "`uv run ruff check .` exits 0 against the skeleton"
    - "`uv run ruff format --check .` exits 0 against the skeleton"
    - "`uv run mypy lib tests` exits 0 against the skeleton"
    - "`uv run pytest -q` exits 0 (at least one smoke test passes)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Python 3.12 project metadata + locked stack (mediapipe, opencv-python, garmin-fit-sdk, polars, google-cloud-storage, google-cloud-bigquery, streamlit, scipy) and dev tools (ruff, mypy, pytest, pytest-cov); ruff + mypy + pytest configuration tables"
      contains: "requires-python = \">=3.12,<3.13\""
    - path: "uv.lock"
      provides: "Reproducible lockfile generated by `uv lock`"
    - path: "lib/vision/__init__.py"
      provides: "Empty namespace for the shared package every later phase imports"
    - path: "tests/__init__.py"
      provides: "Marks tests as a package"
    - path: "tests/test_imports.py"
      provides: "Smoke test importing `vision` and asserting Python version is 3.12.x"
  key_links:
    - from: "tests/test_imports.py"
      to: "lib/vision/__init__.py"
      via: "`import vision`"
      pattern: "import vision"
    - from: "pyproject.toml [tool.hatch.build.targets.wheel] or [tool.uv]"
      to: "lib/vision/"
      via: "package path declaration so `uv sync` installs `vision` into the venv"
      pattern: "packages = \\[\"lib/vision\"\\]|package-dir|\\[tool.hatch.build.targets.wheel\\]"
---

<objective>
Establish the Python 3.12 toolchain that every subsequent phase depends on: `pyproject.toml` pinning the locked research stack, `uv.lock` for reproducibility, ruff/mypy/pytest configuration, an importable `lib/vision/` namespace package, and a smoke test proving everything wires together.

Purpose: BOOT-01. Without this, no later phase can run `uv sync`, no CI gate can lint or type-check, and the shared `lib/vision/` package (Phase 2+) has no home.

Output: A repo where `uv sync && uv run ruff check . && uv run mypy lib tests && uv run pytest -q` all succeed from a clean clone.
</objective>

<execution_context>
@/Users/callum/software-local/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callum/software-local/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/callum/software-local/vision/.planning/STATE.md
@/Users/callum/software-local/vision/.planning/ROADMAP.md
@/Users/callum/software-local/vision/.planning/REQUIREMENTS.md
@/Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md
@/Users/callum/software-local/vision/.planning/research/STACK.md
@/Users/callum/software-local/vision/.planning/research/ARCHITECTURE.md
@/Users/callum/software-local/vision/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write pyproject.toml + create lib/vision skeleton + smoke test</name>
  <files>pyproject.toml, uv.lock, lib/vision/__init__.py, tests/__init__.py, tests/test_imports.py</files>
  <read_first>
    - /Users/callum/software-local/vision/.planning/research/STACK.md (locked versions, "What NOT to Use" list)
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-21..D-24 (CI scope) and §Claude's Discretion (package list latitude)
    - /Users/callum/software-local/vision/.planning/ROADMAP.md §Phase 0 success criterion #1
  </read_first>
  <behavior>
    - `uv run python -c "import vision"` exits 0 and prints nothing
    - `uv run pytest -q tests/test_imports.py` collects at least one test and exits 0
    - The smoke test asserts `sys.version_info[:2] == (3, 12)` so a wrong-Python venv fails loudly
    - `uv run ruff check .` exits 0 (no lint errors in the skeleton)
    - `uv run ruff format --check .` exits 0 (skeleton is already formatted)
    - `uv run mypy lib tests` exits 0 (no type errors in the skeleton)
  </behavior>
  <action>
    Create `pyproject.toml` at the repo root with:

    - `[project]` table: `name = "vision"`, `version = "0.1.0"`, `description = "Cycling form & performance analyzer — pose + telemetry on GCP"`, `requires-python = ">=3.12,<3.13"` (the upper bound is load-bearing per STACK.md — MediaPipe has no 3.13 wheels), `readme = "README.md"`, `license = { text = "MIT" }`, `authors = [{ name = "Callum" }]`.
    - `dependencies = []` — leave the runtime stack empty for Phase 0. Heavy ML deps (mediapipe, opencv-python, garmin-fit-sdk, polars, scipy, pandas, pyarrow, pandas-gbq, google-cloud-storage, google-cloud-bigquery, openmeteo-requests, pydantic, typer, streamlit, plotly) go into optional dependency groups so Phase 0 CI doesn't drag the ML wheels.
    - `[project.optional-dependencies]` with these groups (versions per STACK.md):
      - `pose = ["mediapipe>=0.10.35,<0.11", "opencv-python>=4.10,<4.11"]`
      - `telemetry = ["garmin-fit-sdk>=21.205,<22"]`
      - `data = ["polars>=1.40,<2", "pandas>=2.2,<3", "pyarrow>=17,<18", "pandas-gbq>=0.24,<0.25", "scipy>=1.14,<2", "numpy>=2.0,<3"]`
      - `gcp = ["google-cloud-storage>=3.10,<4", "google-cloud-bigquery>=3,<4"]`
      - `weather = ["openmeteo-requests>=1,<2"]`
      - `viewer = ["streamlit>=1.55,<2", "plotly>=5.24,<6"]`
      - `cli = ["typer>=0.12,<0.13", "pydantic>=2,<3"]`
      - `all = [ ... ]` aggregating the above groups via self-reference (`"vision[pose,telemetry,data,gcp,weather,viewer,cli]"`) so a single `uv sync --extra all` installs everything when needed.
    - `[dependency-groups]` (PEP 735, supported by uv) with a `dev` group: `["ruff>=0.6,<0.7", "mypy>=1.11,<2", "pytest>=8,<9", "pytest-cov>=5,<6"]`. Note explicitly: NO `python-fitparse` (STACK.md says DO NOT USE), NO `mediapipe-silicon`, NO `requirements.txt`.
    - `[build-system]` table: `requires = ["hatchling"]`, `build-backend = "hatchling.build"`.
    - `[tool.hatch.build.targets.wheel]` with `packages = ["lib/vision"]` so the `vision` namespace package resolves from `lib/vision/` even though it's under a non-standard prefix.
    - `[tool.ruff]` table: `line-length = 100`, `target-version = "py312"`, `extend-exclude = ["uv.lock"]`.
    - `[tool.ruff.lint]` table: `select = ["E", "F", "I", "B", "UP", "SIM", "RUF"]` (a sensible curated rule set; nothing too noisy for a greenfield repo).
    - `[tool.ruff.format]` table: leave defaults (double-quote, trailing-comma magic).
    - `[tool.mypy]` table: `python_version = "3.12"`, `strict = true`, `warn_unused_ignores = true`, `disallow_untyped_defs = true`, `files = ["lib", "tests"]`, `exclude = ["infra/kill-switch/"]` (vendored upstream code is not ours to retype).
    - `[tool.pytest.ini_options]` table: `testpaths = ["tests"]`, `addopts = "-ra"`, `pythonpath = ["lib"]` so `import vision` works without an editable install detour.

    Create `lib/vision/__init__.py` with the single line `"""Shared vision package — schemas, helpers, conventions used by every pipeline stage."""` followed by `__version__ = "0.1.0"`. No other code.

    Create `tests/__init__.py` as an empty file.

    Create `tests/test_imports.py`:
    - `import sys`
    - `import vision`
    - `def test_python_is_312() -> None:` asserting `sys.version_info[:2] == (3, 12)`.
    - `def test_vision_importable() -> None:` asserting `vision.__version__ == "0.1.0"`.

    Run `uv lock` then `uv sync` (this populates `.venv` and writes `uv.lock`). Then run `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy lib tests`, and `uv run pytest -q` and ensure all four exit 0. Do NOT install any optional groups in this task — the dev group plus the empty base `dependencies` is sufficient.

    Commit `uv.lock` to the repo (required by D-21 implicit reproducibility goal).
  </action>
  <verify>
    <automated>cd /Users/callum/software-local/vision && uv sync && uv run ruff check . && uv run ruff format --check . && uv run mypy lib tests && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` exists at the repo root and is parseable (`uv run python -c "import tomllib; tomllib.loads(open('pyproject.toml').read())"` exits 0).
    - `pyproject.toml` has `requires-python = ">=3.12,<3.13"` verbatim (grep: `grep -F 'requires-python = ">=3.12,<3.13"' pyproject.toml | wc -l` equals 1).
    - `pyproject.toml` does NOT contain `python-fitparse` (grep: `grep -F 'python-fitparse' pyproject.toml | wc -l` equals 0).
    - `pyproject.toml` lists `mediapipe`, `opencv-python`, `garmin-fit-sdk`, `polars`, `google-cloud-storage`, `google-cloud-bigquery`, `streamlit`, `scipy`, `pandas`, `pyarrow`, `openmeteo-requests`, `pydantic`, `typer` somewhere in optional-dependencies (one grep per package, each `>= 1`).
    - `pyproject.toml` lists `ruff`, `mypy`, `pytest`, `pytest-cov` somewhere in `[dependency-groups].dev` (each `grep -c >= 1`).
    - `uv.lock` exists and is non-empty (`test -s uv.lock`).
    - `lib/vision/__init__.py` exists and exports `__version__ = "0.1.0"`.
    - `tests/test_imports.py` exists and contains both `test_python_is_312` and `test_vision_importable`.
    - All four CI gates pass: `uv run ruff check .` exits 0, `uv run ruff format --check .` exits 0, `uv run mypy lib tests` exits 0, `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>
    `uv sync` populates the venv from a clean clone; ruff/mypy/pytest all exit 0; `lib/vision/` is a real importable package; the smoke test fails loudly if Python isn't 3.12.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev shell → uv → PyPI | uv resolves dependencies from PyPI; lockfile is the integrity boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | uv-installed dependencies | mitigate | Commit `uv.lock` to the repo so subsequent `uv sync --frozen` rejects unexpected version drift; `requires-python = ">=3.12,<3.13"` prevents accidental 3.13 wheel substitution that would silently break MediaPipe in later phases |
| T-00-02 | Information disclosure | pyproject.toml | accept | Public-repo (D-25) — pyproject contains no secrets; the file is intentionally public |
| T-00-SC | Tampering | PyPI installs | mitigate | RESEARCH.md does not yet contain a `## Package Legitimacy Audit` table; Phase 0 only installs first-party canonical packages (ruff/mypy/pytest from astral/python/pytest org; mediapipe from Google; garmin-fit-sdk from Garmin; cloud libs from Google). Treat as `[ASSUMED]` and add a Phase 1 follow-up to formalize the audit when ML/cloud deps actually install. Phase 0 does NOT execute `uv sync --extra all` so the ML wheels do not touch the system in this phase. |

</threat_model>

<verification>
- `uv sync` from a clean clone exits 0 and produces `.venv/`
- `uv run ruff check .` exits 0
- `uv run ruff format --check .` exits 0
- `uv run mypy lib tests` exits 0
- `uv run pytest -q` exits 0 and shows at least 2 passing tests
- `python -c "import sys; assert sys.version_info[:2] == (3,12)"` succeeds inside the venv
</verification>

<success_criteria>
ROADMAP Phase 0 success criterion #1 is met: `uv sync` reproduces the dev environment from `pyproject.toml` + `uv.lock`, and `ruff` + `mypy` + `pytest` all run cleanly against the skeleton.
</success_criteria>

<output>
Create `.planning/phases/00-bootstrap-cost-guardrails/00-01-SUMMARY.md` when done. Record exact tool versions resolved by `uv lock` (read from `uv.lock`) so later phases have a known-good baseline.
</output>
