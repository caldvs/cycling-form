---
phase: 00-bootstrap-cost-guardrails
plan: 06
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - README.md
  - .github/workflows/ci.yaml
autonomous: true
requirements:
  - BOOT-01
  - BOOT-06
tags:
  - docs
  - ci
  - portfolio

must_haves:
  truths:
    - "`README.md` exists at repo root with a JD-bullet → code mapping table containing one row per JD area (CS/Engineering, Computer vision / pose estimation, GCP-based ML workloads, Sport/performance telemetry)"
    - "Each JD-mapping row has `Status: placeholder` initially (D-19)"
    - "README contains a 'What this does NOT do' section header (D-20) — empty under it for now"
    - "README contains a CI-badge URL referencing GitHub Actions (D-23)"
    - "`.github/workflows/ci.yaml` exists, uses `astral-sh/setup-uv`, runs `ruff check`, `ruff format --check`, `mypy`, `pytest -q` on push and PR to main"
    - "CI workflow is valid YAML (parseable)"
  artifacts:
    - path: "README.md"
      provides: "Project intro, JD-mapping table with placeholder rows, 'what this does NOT do' header, CI badge URL, architecture-diagram placeholder"
      contains: "JD"
      min_lines: 60
    - path: ".github/workflows/ci.yaml"
      provides: "GitHub Actions CI — lint + type + test on push/PR to main, single Python 3.12 matrix"
      contains: "astral-sh/setup-uv"
  key_links:
    - from: ".github/workflows/ci.yaml"
      to: "pyproject.toml + uv.lock"
      via: "`uv sync --frozen` step using committed lockfile"
      pattern: "uv sync"
    - from: ".github/workflows/ci.yaml"
      to: "tests/test_imports.py"
      via: "`uv run pytest -q` step"
      pattern: "pytest"
    - from: "README.md"
      to: ".github/workflows/ci.yaml"
      via: "embedded CI status badge image referencing the workflow file by name"
      pattern: "ci.yaml/badge.svg"
---

<objective>
Author the README skeleton (BOOT-06) with the JD-bullet → code mapping table that downstream phases will fill in, and the minimal CI workflow (BOOT-01 CI side / D-21..D-24) that runs ruff + mypy + pytest on every push and PR. The CI badge points at the workflow defined in this plan, so the badge is green-after-first-commit (a high-signal portfolio detail per D-23).

Purpose: BOOT-06 + the CI half of BOOT-01. ROADMAP Phase 0 success criterion #5 satisfied. JD-signal coverage matrix delivered as a hiring-manager-friendly table per D-18 (skimmable, audit-friendly).

Output: A README.md that a hiring manager can read top-down in 60 seconds plus a CI workflow that gates every push.
</objective>

<execution_context>
@/Users/callum/software-local/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callum/software-local/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/callum/software-local/vision/.planning/STATE.md
@/Users/callum/software-local/vision/.planning/ROADMAP.md
@/Users/callum/software-local/vision/.planning/REQUIREMENTS.md
@/Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md
@/Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-01-SUMMARY.md
@/Users/callum/software-local/vision/pyproject.toml
@/Users/callum/software-local/vision/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md skeleton with JD-mapping table</name>
  <files>README.md</files>
  <read_first>
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-18..D-20 (JD-mapping table layout, placeholder status, 'What this does NOT do' header)
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §specifics (table sits ABOVE the architecture diagram, not below — hiring manager reads top-down)
    - /Users/callum/software-local/vision/.planning/ROADMAP.md §Phase 0 success criterion #5
    - /Users/callum/software-local/vision/CLAUDE.md §Project (core value, constraints) — read for tone/positioning
  </read_first>
  <action>
    Create `README.md` at the repo root. Structure top-down (the hiring manager reads top-down per CONTEXT §specifics — JD-mapping table goes ABOVE architecture-diagram placeholder).

    Block 1 — Title + CI badge + one-paragraph positioning. Use H1 for the title and place the CI badge image right under it. The badge URL must reference `workflows/ci.yaml/badge.svg`. Include the placeholders `<OWNER>/<REPO>` in the URL with a one-line comment above the badge: TODO-style note saying these placeholders get replaced once the public repo is created (per D-26 in CONTEXT.md). Follow with a 4–6-line paragraph stating the project's core value (per CLAUDE.md §Project): given a video of an indoor-trainer ride and its FIT file, the project produces explainable per-stroke pose-vs-telemetry correlations such as the "knee-over-pedal-spindle drift correlates with power drop after minute 20" example; name the four JD areas (computer vision / pose estimation, GCP-based ML workloads, sport/performance telemetry, CS/Engineering practices).

    Block 2 — JD-bullet → code mapping (THE load-bearing table, per D-18). H2 heading `## JD-bullet → code mapping`. Then a Markdown table with EXACTLY four data rows (one per JD area) and these four columns: `JD area`, `Demonstrated by`, `Code/doc references`, `Status`. Each row's `Status` cell starts as the literal string `placeholder` (D-19); later phases overwrite the other two columns as they ship. The four rows are:

    - Row 1: `CS/Engineering` | `(filled by Phase 0 + Phase 2 + Phase 6 — toolchain, container hygiene, CI/CD)` | `` `pyproject.toml`, `.github/workflows/ci.yaml`, `pipeline/*/Dockerfile`, `tests/` `` | `placeholder`
    - Row 2: `Computer vision / pose estimation` | `(filled by Phase 1 + Phase 5 — MediaPipe Tasks API, One-Euro filter, visibility gating, per-stroke metrics, annotated frame strip)` | `` `lib/vision/`, `pipeline/pose/`, `pipeline/features/`, `tests/test_pose*.py`, `docs/filming-protocol.md` `` | `placeholder`
    - Row 3: `GCP-based ML workloads` | `(filled by Phase 3 + Phase 4 + Phase 5 — Cloud Run Jobs, GCS, BigQuery, Workflows, Eventarc, Streamlit viewer on Cloud Run Service)` | `` `pipeline/*/`, `infra/workflows/`, `infra/bigquery/`, `scripts/bootstrap-gcp.sh`, `viewer/` `` | `placeholder`
    - Row 4: `Sport/performance telemetry` | `(filled by Phase 1 + Phase 3 — official Garmin FIT SDK, pause-event handling, indoor/outdoor detection, gear inference ±1 cog, Open-Meteo enrichment, BigQuery telemetry_raw + fused_timeline view)` | `` `pipeline/fit/`, `lib/vision/schemas/`, `tests/test_fit*.py` `` | `placeholder`

    Block 3 — `## What this does NOT do` (empty section header per D-20). Include a one-line lead-in sentence stating that the project intentionally avoids certain anti-features and that Phase 6 (PORT-02) fills the bullet list from `.planning/REQUIREMENTS.md` § "Out of Scope". Leave the bullet list itself empty for Phase 6 to fill.

    Block 4 — `## Architecture`. One paragraph describing the four-stage batch pipeline (pose → fit → features → correlate) running as Cloud Run Jobs with GCS as the durable artifact bus and BigQuery as the analytical sink, fronted by a Streamlit viewer. Note that Phase 6 (PORT-04) embeds the rendered architecture diagram here; for now reference `.planning/research/ARCHITECTURE.md` for the detailed diagram. Add an HTML-comment placeholder line: `<!-- TODO Phase 6 (PORT-04): embed mermaid/static architecture diagram here -->`.

    Block 5 — `## Local development` quickstart. Three steps as fenced code blocks (language hint `bash`):
    1. Install uv: `curl -LsSf https://astral.sh/uv/install.sh | sh`
    2. Sync env: `uv sync`
    3. Run gates: `uv run ruff check . && uv run mypy lib tests && uv run pytest -q`

    One trailing line: `See CONTRIBUTING.md for the full local-development policy and the 'never commit secrets' rules.`

    Block 6 — `## GCP setup (Phase 0)` (forward-reference for plan 07). Three steps as a numbered list referencing the artifacts created by plans 02/03/04:
    1. `cp scripts/bootstrap-gcp.env.example scripts/bootstrap-gcp.env`
    2. Fill in `GCP_PROJECT_ID`, `ALERT_EMAIL`, and `BILLING_ACCOUNT_ID` in the new env file.
    3. `./scripts/bootstrap-gcp.sh` (creates the project, budget, kill-switch).

    Follow with one load-bearing warning line: run `./scripts/test-kill-switch.sh` against a throwaway project once before relying on the kill switch on the live project; refer to `infra/kill-switch/README.md` for the one-way-door warning.

    Block 7 — `## Layout`. A short tree (under 30 lines) inside a fenced code block reflecting Phase-0 reality plus signposts for Phase 1+. Each line should be a path with a trailing comment noting which phase owns it. Include at minimum: `.github/workflows/ci.yaml`, `docs/filming-protocol.md`, `infra/kill-switch/`, `lib/vision/`, `scripts/bootstrap-gcp.sh`, `scripts/test-kill-switch.sh`, `tests/`, `pyproject.toml + uv.lock`, plus forward-looking entries for `pipeline/{pose,fit,features,correlate}/` (Phase 2), `viewer/` (Phase 5), and `.planning/`.

    Block 8 — `## License & credits`. Two bullets: License = MIT (matching `pyproject.toml`); vendored components — see `NOTICE`, currently includes the Cyclenerd billing kill switch (Apache-2.0).

    Verify the file's line count is between 60 and 200; verify fenced code-block markers are balanced (even count of triple-backtick lines).
  </action>
  <verify>
    <automated>cd /Users/callum/software-local/vision && test -f README.md && [ "$(wc -l < README.md)" -ge 60 ] && [ "$(wc -l < README.md)" -le 200 ] && grep -F 'Vision' README.md && grep -F 'CS/Engineering' README.md && grep -F 'Computer vision / pose estimation' README.md && grep -F 'GCP-based ML workloads' README.md && grep -F 'Sport/performance telemetry' README.md && [ "$(grep -c '| placeholder |' README.md)" -ge 4 ] && grep -F 'What this does NOT do' README.md && grep -F 'workflows/ci.yaml/badge.svg' README.md && grep -F 'uv sync' README.md && grep -F 'bootstrap-gcp.sh' README.md && grep -F 'test-kill-switch.sh' README.md && grep -F 'docs/filming-protocol.md' README.md && [ "$(($(grep -c '^```' README.md) % 2))" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at repo root.
    - Line count in 60–200 range.
    - Contains exactly four (or more) `| placeholder |` cells corresponding to the four JD-mapping rows (D-19).
    - JD-mapping table appears in the file BEFORE the `## Architecture` section header (verifiable via `awk '/## JD-bullet/{jd=NR} /## Architecture/{arc=NR} END{exit !(jd && arc && jd<arc)}' README.md`).
    - 'What this does NOT do' section header present (D-20) with the bullet list left empty for Phase 6 (PORT-02).
    - CI badge URL references `workflows/ci.yaml/badge.svg` (D-23).
    - Local-development quickstart references `uv sync` and `uv run pytest -q`.
    - GCP-setup section references `scripts/bootstrap-gcp.sh` AND `scripts/test-kill-switch.sh`.
    - Repository-layout block references all Phase-0 deliverables.
    - Fenced code-block markers are balanced.
  </acceptance_criteria>
  <done>
    A hiring manager visiting the repo sees the JD-mapping table within the first scroll, understands the project's claims and which are filled vs placeholder, and can run the three commands under "Local development" to reproduce the env.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .github/workflows/ci.yaml (ruff + mypy + pytest on push/PR)</name>
  <files>.github/workflows/ci.yaml</files>
  <read_first>
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-21..D-24 (CI scope: lint + type + test; single-version Python 3.12; build/push deferred to Phase 6; CI badge live from day 1)
    - /Users/callum/software-local/vision/pyproject.toml (the gates this CI runs)
    - quality-gate: CI workflow MUST use `astral-sh/setup-uv` action
  </read_first>
  <action>
    Create `.github/workflows/ci.yaml`. The directory `.github/workflows/` may not exist yet; create it with `mkdir -p`.

    Workflow structure:
    - `name: CI` at the top.
    - Trigger `on:` both `push` to `branches: [main]` and `pull_request` to `branches: [main]`.
    - Top-level `permissions: contents: read` (least-privilege; CI does not need write).
    - Single job named `ci` (`name: ruff + mypy + pytest (Python 3.12)`) running on `ubuntu-latest`, with `timeout-minutes: 10`.
    - Steps (in this exact order):
      1. `Checkout` — `uses: actions/checkout@v4`
      2. `Install uv` — `uses: astral-sh/setup-uv@v3` with `with:` block setting `version: "0.4.x"` (pinned for reproducibility; bump deliberately), `enable-cache: true`, and `cache-dependency-glob:` multi-line YAML containing both `pyproject.toml` and `uv.lock`.
      3. `Set up Python 3.12` — `run: uv python install 3.12`
      4. `Sync dev environment (frozen against uv.lock)` — `run: uv sync --frozen`
      5. `Lint (ruff check)` — `run: uv run ruff check .`
      6. `Format check (ruff format --check)` — `run: uv run ruff format --check .`
      7. `Type check (mypy)` — `run: uv run mypy lib tests`
      8. `Test (pytest)` — `run: uv run pytest -q`

    The workflow does NOT build container images, push artifacts, or run on tagged releases — those are explicitly deferred to Phase 6 per D-22.

    After writing the file, validate it as YAML. Use `uv tool run --from yamllint yamllint -d relaxed .github/workflows/ci.yaml` — `uv tool run` fetches `yamllint` on-demand without polluting the project's dev dependency group. Exit code 0 (no errors; warnings about line length acceptable). If `uv tool run` is unavailable in the executor's environment, fall back to `uv run --with pyyaml python -c "import yaml, sys; yaml.safe_load(open('.github/workflows/ci.yaml')); print('OK')"` — this uses `uv run`'s `--with` flag to add `pyyaml` ephemerally.
  </action>
  <verify>
    <automated>cd /Users/callum/software-local/vision && test -f .github/workflows/ci.yaml && grep -F 'astral-sh/setup-uv' .github/workflows/ci.yaml && grep -F 'uv sync --frozen' .github/workflows/ci.yaml && grep -F 'uv run ruff check' .github/workflows/ci.yaml && grep -F 'uv run ruff format --check' .github/workflows/ci.yaml && grep -F 'uv run mypy lib tests' .github/workflows/ci.yaml && grep -F 'uv run pytest -q' .github/workflows/ci.yaml && grep -E 'branches:\s*\[main\]' .github/workflows/ci.yaml && grep -F 'ubuntu-latest' .github/workflows/ci.yaml && grep -F 'timeout-minutes:' .github/workflows/ci.yaml && uv run --with pyyaml python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yaml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yaml` exists and parses as valid YAML.
    - References `astral-sh/setup-uv` (per quality gate).
    - Has steps `uv sync --frozen`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy lib tests`, `uv run pytest -q` (one grep per step; each `>= 1` match).
    - Triggered on `push` AND `pull_request` to `main`.
    - Runs on `ubuntu-latest` with a `timeout-minutes` ceiling.
    - Does NOT contain any container-build steps (`docker build`, `docker push`, `gcr.io`, `artifact-registry`) — these are Phase 6 (D-22). Verify by `! grep -E 'docker (build|push)|gcr\.io|artifactregistry' .github/workflows/ci.yaml`.
  </acceptance_criteria>
  <done>
    A push to `main` triggers a CI run that exercises the four gates from plan 01 (ruff check, ruff format --check, mypy, pytest). The badge in `README.md` reflects the live status. Build/push is deferred to Phase 6 per D-22.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub-hosted runner → repository checkout | Workflow runs against arbitrary push content; pull_request from forks runs with limited token scope by default |
| README badge URL → public web | Anyone can see CI status; this is intentional portfolio signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-16 | Tampering | Third-party GitHub Action versions (`actions/checkout@v4`, `astral-sh/setup-uv@v3`) | mitigate | Pin to major version (`@v4`, `@v3`); `astral-sh/setup-uv` further pinned to `version: "0.4.x"` minor range in the `with:` block. SHA-pinning is deferred to Phase 6 hardening — `@vN` is the documented convention and acceptable for Phase 0 |
| T-00-17 | Elevation of privilege | Workflow token permissions | mitigate | Top-level `permissions: contents: read` restricts the workflow's `GITHUB_TOKEN` to read-only; no PR comments, no releases, no package writes in Phase 0 |
| T-00-18 | Denial of service | Long-running test hangs CI | mitigate | `timeout-minutes: 10` caps the job; pytest gates exit fast given no real tests yet |
| T-00-SC | Tampering | `astral-sh/setup-uv@v3` is a third-party action — supply-chain concern | accept | Astral (uv's maintainer) is the canonical source per RESEARCH.md/STACK.md; quality gate explicitly mandates this action. SHA-pinning is a Phase-6 hardening item (PORT-05). |

</threat_model>

<verification>
- `README.md` line count 60–200; all four JD-mapping rows present with `placeholder` Status; JD-table appears before Architecture section; CI badge URL present
- `.github/workflows/ci.yaml` parses as valid YAML, uses `astral-sh/setup-uv`, runs `ruff check`, `ruff format --check`, `mypy`, `pytest`
- No container-build steps in the CI file (Phase 6 deferral)
- Workflow restricted to read-only `GITHUB_TOKEN` permissions
</verification>

<success_criteria>
BOOT-06 met: README skeleton with the JD-bullet → code mapping table that downstream phases fill in. BOOT-01 CI side met: a green-after-first-commit CI badge backed by a workflow that runs ruff + mypy + pytest. ROADMAP Phase 0 success criterion #5 satisfied.
</success_criteria>

<output>
Create `.planning/phases/00-bootstrap-cost-guardrails/00-06-SUMMARY.md` when done. Record the exact CI badge URL pattern used (so Phase 6 PORT-05 can extend the workflow without breaking the badge), and any minor version drift in the pinned action versions.
</output>
