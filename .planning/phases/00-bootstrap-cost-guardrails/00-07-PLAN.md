---
phase: 00-bootstrap-cost-guardrails
plan: 07
type: execute
wave: 3
depends_on:
  - 00-04
  - 00-06
files_modified:
  - .planning/phases/00-bootstrap-cost-guardrails/region.log
autonomous: false
requirements:
  - BOOT-02
  - BOOT-03
  - BOOT-04
tags:
  - operator
  - gcp
  - manual
  - kill-switch
  - human-action

must_haves:
  truths:
    - "A GCP project exists, is linked to a billing account, has a $20/month budget, and has email alerts at 50/90/100% (BOOT-02)"
    - "The billing kill-switch Cloud Function (Gen 2) is deployed and ACTIVE, subscribed to the budget's Pub/Sub topic (BOOT-03)"
    - "`scripts/test-kill-switch.sh` has been run against a throwaway project and printed `KILL-SWITCH TEST: PASS` (BOOT-03 — the load-bearing end-to-end test per ROADMAP success criterion #3)"
    - "All Phase-0 resources (project default region, kill-switch function region, future Cloud Run / BQ resources) are pinned to the same `$GCP_REGION` from `scripts/bootstrap-gcp.env` (BOOT-04)"
    - "The operator has verified that the budget appears in the GCP console with the correct amount, thresholds, and notification channels"
  artifacts:
    - path: ".planning/phases/00-bootstrap-cost-guardrails/region.log"
      provides: "One-line marker recording the chosen GCP region — proof of BOOT-04 region-pin audit"
      contains: "Region:"
  key_links:
    - from: "scripts/bootstrap-gcp.env (operator-filled)"
      to: "live GCP project"
      via: "scripts/bootstrap-gcp.sh"
      pattern: "GCP_PROJECT_ID"
    - from: "scripts/test-kill-switch.sh (run against throwaway project)"
      to: "Cloud Function logs + gcloud billing describe"
      via: "synthetic Pub/Sub budget-exceeded message"
      pattern: "KILL-SWITCH TEST: PASS"
---

<objective>
Operator-only execution of the GCP setup scripts authored in plans 03 and 04. This plan is `autonomous: false` because it requires live GCP access, a billing account, and interaction with the GCP console / inbox — none of which Claude can do on the operator's behalf.

Purpose: Close BOOT-02, BOOT-03, BOOT-04 by actually running the scripts against live GCP infrastructure. ROADMAP success criterion #3 ("kill switch deployed AND tested end-to-end") is satisfied here.

Output: A live, cost-bounded GCP project ready for Phase 3 to start using (Phase 1 is local-only, so the GCP project sits idle until Phase 3 brings it online).

**Operator pre-flight:** Plans 01–06 should be COMPLETE and CI should be green on the public repo before starting this plan. Plans 01–06 produce all source-of-truth files this plan consumes.
</objective>

<execution_context>
@/Users/callum/software-local/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callum/software-local/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/callum/software-local/vision/.planning/STATE.md
@/Users/callum/software-local/vision/.planning/ROADMAP.md
@/Users/callum/software-local/vision/.planning/REQUIREMENTS.md
@/Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md
@/Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-03-SUMMARY.md
@/Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-04-SUMMARY.md
@/Users/callum/software-local/vision/scripts/bootstrap-gcp.env.example
@/Users/callum/software-local/vision/scripts/bootstrap-gcp.sh
@/Users/callum/software-local/vision/scripts/test-kill-switch.sh
@/Users/callum/software-local/vision/infra/kill-switch/README.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator pre-flight — confirm prerequisites before any GCP commands</name>
  <files>(none — read-only environment checks)</files>
  <read_first>
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-06 (throwaway project for the kill-switch test), §D-07 (default us-central1), §D-10 (Europe override), §D-11 (ADC auth path)
    - /Users/callum/software-local/vision/scripts/bootstrap-gcp.env.example (env vars the operator needs to fill in)
  </read_first>
  <action>
    PAUSE — this is an operator task. The executor agent prints the verification checklist below and waits for the operator to confirm each item before resuming. No autonomous file edits in this task.

    Operator's checklist — perform on your local workstation:

    1. **Repo state:** Confirm all Phase-0 files exist:
       ```
       ls scripts/bootstrap-gcp.sh scripts/test-kill-switch.sh scripts/bootstrap-gcp.env.example infra/kill-switch/main.py docs/filming-protocol.md README.md .github/workflows/ci.yaml pyproject.toml uv.lock
       ```
       All eight paths must exist.

    2. **gcloud installed and authenticated:** Run:
       ```
       gcloud --version | head -n1
       gcloud auth list --filter=status:ACTIVE --format="value(account)"
       gcloud auth application-default print-access-token >/dev/null && echo "ADC OK"
       ```
       The third command must print `ADC OK`. If not, run `gcloud auth login` AND `gcloud auth application-default login` first (per D-11).

    3. **Billing account available:** Run:
       ```
       gcloud billing accounts list --filter='OPEN=True' --format='table(ACCOUNT_ID, NAME, OPEN)'
       ```
       At least one OPEN billing account must be listed. Capture the ACCOUNT_ID (format `XXXXXX-XXXXXX-XXXXXX`) — you'll need it for the env file.

    4. **Project id and region decided:** Default `GCP_REGION=us-central1` per D-07. If you're based in Europe/UK, decide NOW whether to switch to `europe-west1` (D-10 — the override must happen BEFORE any service is created). The project id must be globally unique across all of GCP and 6–30 characters of lowercase letters, digits, and hyphens (recommend `vision-prod-<your-suffix>`).

    5. **Throwaway project planned:** Per D-06, the end-to-end kill-switch test MUST run against a throwaway project — NOT the live `vision-prod-*` project. Decide on a throwaway project id now (e.g., `vision-killtest-<random>`). You will set up this throwaway project, run the kill-switch test against it, observe billing get disabled, then either delete the throwaway or leave it disabled.

    All five checks must pass before continuing to Task 2.
  </action>
  <verify>
    Operator confirms readiness via the resume-signal below. The five-item checklist above is the verification rubric.
  </verify>
  <done>
    Operator types the resume signal indicating preflight is complete. No file artifacts are produced by this task.
  </done>
  <resume-signal>Type "preflight OK" once all five checks pass. If a check fails, fix it and re-confirm before resuming.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Bootstrap the live GCP project (run scripts/bootstrap-gcp.sh)</name>
  <files>scripts/bootstrap-gcp.env (operator creates locally; gitignored per plan 02)</files>
  <read_first>
    - /Users/callum/software-local/vision/scripts/bootstrap-gcp.env.example (template; copy to bootstrap-gcp.env and fill in)
    - /Users/callum/software-local/vision/scripts/bootstrap-gcp.sh (the script you're about to run; skim it before executing)
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-02 (script is idempotent; safe to re-run)
  </read_first>
  <action>
    PAUSE — operator task. The executor agent prints the procedure below and waits.

    Step 1 — Create the env file and fill it in:
    ```
    cd /Users/callum/software-local/vision
    cp scripts/bootstrap-gcp.env.example scripts/bootstrap-gcp.env
    ```
    Open `scripts/bootstrap-gcp.env` in an editor and fill in:
    - `GCP_PROJECT_ID=` — your chosen project id (e.g., `vision-prod-callum`).
    - `GCP_REGION=us-central1` — change to `europe-west1` HERE if D-10 applies to you; do NOT change it later.
    - `GCP_BQ_LOCATION=US` — change to `EU` if region is `europe-west1`.
    - `ALERT_EMAIL=dvscllm@gmail.com` — your alert-receiving email.
    - `BILLING_ACCOUNT_ID=` — the ACCOUNT_ID from Task 1 step 3.

    Leave the `KILL_SWITCH_*` defaults as-is.

    Step 2 — Run the script:
    ```
    ./scripts/bootstrap-gcp.sh
    ```

    Confirm at the `Continue? [y/N]` prompt. The script will:
    - Create the project (or skip if it already exists)
    - Link billing
    - Enable all required APIs (cloudbilling, billingbudgets, pubsub, cloudfunctions, run, cloudbuild, artifactregistry, eventarc, logging, iam)
    - Create the Pub/Sub topic `vision-billing-alerts`
    - Create the kill-switch service account + grant `roles/billing.projectManager`
    - Deploy the Cloud Function (Gen 2) from `infra/kill-switch/`
    - Create the email Monitoring notification channel (if missing)
    - Create the $20/month budget with alerts at 50/90/100%
    - Set default compute/run regions

    Total runtime: ~5–10 minutes (the Cloud Function deploy is the slowest step).

    Step 3 — Post-run verification commands (each must return the expected output; export `GCP_PROJECT_ID`, `GCP_REGION`, `BILLING_ACCOUNT_ID` in your shell first):

    ```
    # Project exists and billing is enabled
    gcloud projects describe "$GCP_PROJECT_ID" --format='value(projectId)'
    gcloud billing projects describe "$GCP_PROJECT_ID" --format='value(billingEnabled)'   # expect: True

    # Required APIs enabled
    gcloud services list --enabled --project="$GCP_PROJECT_ID" --format='value(NAME)' | grep -E 'pubsub|cloudfunctions|billingbudgets|run|iam'   # expect: 5+ lines

    # Pub/Sub topic exists
    gcloud pubsub topics describe vision-billing-alerts --project="$GCP_PROJECT_ID" --format='value(name)'

    # Service account exists with billing-manager role
    SA="vision-killswitch-sa@${GCP_PROJECT_ID}.iam.gserviceaccount.com"
    gcloud projects get-iam-policy "$GCP_PROJECT_ID" --filter="bindings.members:serviceAccount:$SA" --format='value(bindings.role)' | grep 'roles/billing.projectManager'

    # Cloud Function deployed
    gcloud functions describe stop-billing --gen2 --region="$GCP_REGION" --project="$GCP_PROJECT_ID" --format='value(state)'   # expect: ACTIVE

    # Budget exists with all three thresholds
    BUDGET_NAME=$(gcloud billing budgets list --billing-account="$BILLING_ACCOUNT_ID" --filter='displayName=vision-monthly-budget' --format='value(name)')
    gcloud billing budgets describe "$BUDGET_NAME" --billing-account="$BILLING_ACCOUNT_ID" --format='value(thresholdRules[].thresholdPercent)'   # expect: 0.5 / 0.9 / 1.0

    # Default compute/run region set
    gcloud config get-value compute/region
    gcloud config get-value run/region
    ```

    Step 4 — Visual sanity check in the GCP console:
    - Open https://console.cloud.google.com/billing/budgets — confirm `vision-monthly-budget` is listed with the $20 amount and three thresholds.
    - Open https://console.cloud.google.com/functions/list?project=YOUR_GCP_PROJECT_ID — confirm `stop-billing` shows status `Active` and is region-pinned to your chosen region.

    If ANY of the verification commands fails, stop. Re-run `./scripts/bootstrap-gcp.sh` (it's idempotent — re-running is safe). If failures persist, capture the error and resolve before proceeding.
  </action>
  <verify>
    Operator runs the eight post-run verification commands in Step 3 and the two console checks in Step 4; all must pass before the resume-signal is given.
  </verify>
  <done>
    Live GCP project exists with billingEnabled=True, kill-switch function ACTIVE, budget vision-monthly-budget at $20 with 50/90/100% thresholds, default compute/run region set to $GCP_REGION.
  </done>
  <resume-signal>Type "bootstrap OK" once all verification commands and console checks pass. If something failed and you fixed it, re-run the verification commands before resuming.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Test the kill switch end-to-end on a THROWAWAY project (run scripts/test-kill-switch.sh)</name>
  <files>scripts/bootstrap-gcp.env.throwaway, scripts/bootstrap-gcp.env.prod (transient operator-managed env files; gitignored)</files>
  <read_first>
    - /Users/callum/software-local/vision/scripts/test-kill-switch.sh (the script you're about to run; the loud confirmation gate is intentional)
    - /Users/callum/software-local/vision/infra/kill-switch/README.md §## ⚠ This is a one-way door (recovery procedure)
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-06 (throwaway-project requirement is non-negotiable)
  </read_first>
  <action>
    PAUSE — operator task. The executor agent prints the procedure below and waits.

    ⚠ READ BEFORE PROCEEDING: This task runs the kill switch against a project, which DISABLES BILLING and stops all running services. Per D-06, this MUST be run against a THROWAWAY project, not your live `vision-prod-*` project.

    Step 1 — Set up the throwaway project. Copy your env file and edit it for the throwaway:
    ```
    cp scripts/bootstrap-gcp.env scripts/bootstrap-gcp.env.throwaway
    # Edit scripts/bootstrap-gcp.env.throwaway:
    #   GCP_PROJECT_ID=vision-killtest-<random>   (a fresh project id)
    ```

    Step 2 — Bootstrap the throwaway project. Swap the env files so the script reads the throwaway config:
    ```
    mv scripts/bootstrap-gcp.env scripts/bootstrap-gcp.env.prod
    mv scripts/bootstrap-gcp.env.throwaway scripts/bootstrap-gcp.env
    ./scripts/bootstrap-gcp.sh
    ```
    After it finishes, you have a throwaway project mirroring the prod setup.

    Step 3 — Run the kill-switch test against the throwaway:
    ```
    ./scripts/test-kill-switch.sh
    ```

    The script will:
    - Print a loud warning and require you to type `DISABLE BILLING ON <throwaway-project-id>` verbatim to proceed.
    - Capture the pre-test billing state (must be `True`).
    - Publish a synthetic budget-exceeded Pub/Sub message.
    - Poll Cloud Function logs (120 s timeout) for evidence the function ran.
    - Poll billing state (180 s timeout) until `billingEnabled=False`.
    - Print `KILL-SWITCH TEST: PASS` on success.

    Total runtime: typically 30–90 s once the message is published.

    Step 4 — Verify the test result manually (set `THROWAWAY_PROJECT_ID` and `GCP_REGION` in your shell first):
    ```
    gcloud billing projects describe "$THROWAWAY_PROJECT_ID" --format='value(billingEnabled)'   # expect: False
    gcloud functions logs read stop-billing --gen2 --region="$GCP_REGION" --project="$THROWAWAY_PROJECT_ID" --limit=10
    ```
    The logs should show the function received the Pub/Sub event and called `updateBillingInfo`.

    Step 5 — Restore the prod env file:
    ```
    mv scripts/bootstrap-gcp.env scripts/bootstrap-gcp.env.throwaway
    mv scripts/bootstrap-gcp.env.prod scripts/bootstrap-gcp.env
    ```

    Step 6 — Decide what to do with the throwaway project:
    - Recommended: delete it. `gcloud projects delete "$THROWAWAY_PROJECT_ID"` (immediately frees the project id; recoverable for 30 days).
    - Alternatively: leave it billing-disabled (it consumes no resources at $0).

    Step 7 — Final sanity check on the LIVE prod project (it should be untouched by the throwaway test — `PROD_GCP_PROJECT_ID` is your live project id):
    ```
    gcloud billing projects describe "$PROD_GCP_PROJECT_ID" --format='value(billingEnabled)'   # expect: True
    gcloud functions describe stop-billing --gen2 --region="$GCP_REGION" --project="$PROD_GCP_PROJECT_ID" --format='value(state)'   # expect: ACTIVE
    ```

    If the prod project's billing got disabled (it should not have — the kill switch only acts on the project named in the Pub/Sub message), re-enable it immediately: `gcloud billing projects link "$PROD_GCP_PROJECT_ID" --billing-account="$BILLING_ACCOUNT_ID"`. This means the kill-switch code mistakenly affected the wrong project; capture logs and stop here.
  </action>
  <verify>
    `scripts/test-kill-switch.sh` exits 0 with the line `KILL-SWITCH TEST: PASS` printed; throwaway-project `billingEnabled=False`; prod-project `billingEnabled=True` confirmed in Step 7.
  </verify>
  <done>
    BOOT-03 closed (ROADMAP success criterion #3 met): kill switch deployed AND tested end-to-end. Throwaway project either deleted or left disabled. Prod project untouched.
  </done>
  <resume-signal>Type "kill-switch test PASS" once `KILL-SWITCH TEST: PASS` was printed by the script AND the prod project's `billingEnabled=True` is confirmed AND the throwaway project is either deleted or left disabled.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Final region-pin audit + budget-alert sanity check + write region.log</name>
  <files>.planning/phases/00-bootstrap-cost-guardrails/region.log</files>
  <read_first>
    - /Users/callum/software-local/vision/.planning/phases/00-bootstrap-cost-guardrails/00-CONTEXT.md §D-09 (single GCP_REGION as source of truth across all resources)
    - /Users/callum/software-local/vision/.planning/ROADMAP.md §Phase 0 success criterion #2 (budget + alerts at 50/90/100% + region documented)
  </read_first>
  <action>
    PAUSE — operator task. The executor agent prints the procedure below and waits. The only autonomous write is appending one line to `region.log` (handled by the operator via the `echo` command below; gitignored excludes do NOT cover this path, so the file is committable as a Phase-0 audit artifact).

    Step 1 — Region-pin audit (BOOT-04 evidence). Run on the live prod project (export `GCP_REGION` and `GCP_PROJECT_ID` first):
    ```
    # 1. gcloud defaults are pinned
    test "$(gcloud config get-value compute/region)" = "$GCP_REGION" && echo "compute/region OK"
    test "$(gcloud config get-value run/region)" = "$GCP_REGION" && echo "run/region OK"

    # 2. Kill-switch function is in the chosen region
    gcloud functions describe stop-billing --gen2 --region="$GCP_REGION" --project="$GCP_PROJECT_ID" --format='value(state,environment)'

    # 3. Document the region choice (creates a small marker file)
    echo "Region: $GCP_REGION (per D-07; D-10 override applies if you switched) — captured $(date -u '+%Y-%m-%dT%H:%M:%SZ')" >> .planning/phases/00-bootstrap-cost-guardrails/region.log
    ```

    All three commands must succeed: the first two print `OK`; the third appends one line to `region.log`.

    Step 2 — Budget-alert sanity check (BOOT-02 evidence). Open the GCP console: https://console.cloud.google.com/billing/budgets and confirm that the budget shows:
    - Amount: $20 USD
    - Three alert thresholds at 50%, 90%, 100%
    - Email notification channel routing to `$ALERT_EMAIL`
    - Pub/Sub notifications enabled targeting `vision-billing-alerts`

    This is sufficient evidence that the budget+alerts are wired correctly. The alert email itself will arrive the first time real spend crosses 50% — which is exactly the test downstream phases will exercise organically.

    Step 3 — Capture a screenshot of the budget page (Phase 6 PORT-04 reuses this) and save it to a non-tracked location for now. Phase 6 moves it into the repo. (Optional but recommended.)
  </action>
  <verify>
    `region.log` exists at `.planning/phases/00-bootstrap-cost-guardrails/region.log` with at least one line matching `^Region: ` (operator-runnable: `grep -c '^Region:' .planning/phases/00-bootstrap-cost-guardrails/region.log` returns `>= 1`); budget console shows $20 + 50/90/100% thresholds + email channel + Pub/Sub target.
  </verify>
  <done>
    BOOT-02 (budget + alerts) and BOOT-04 (region pin) are closed with documented evidence. `region.log` is the Phase-0 audit artifact.
  </done>
  <resume-signal>Type "region + alerts verified" once the region-pin audit's three commands succeed AND the budget console shows the correct amount + thresholds + notification channels AND `region.log` contains at least one `Region:` line.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator terminal → live GCP | The operator's gcloud auth identity is the trust boundary; mistakes are real and billable |
| `scripts/bootstrap-gcp.env` (gitignored) → script runtime | Contains the billing account id and alert email; never commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-19 | Denial of service | Operator accidentally runs `test-kill-switch.sh` against the prod project | mitigate | `test-kill-switch.sh` (plan 04) requires the operator to type the literal phrase `DISABLE BILLING ON <PROJECT_ID>`; this plan's Task 3 explicitly walks the operator through env-file swapping to ensure the throwaway project is targeted |
| T-00-20 | Information disclosure | The operator's gcloud auth identity has org-wide reach | accept | Solo project; the operator IS the org admin. Risk is bounded by the operator's own carefulness. |
| T-00-21 | Repudiation | No audit trail of which project the bootstrap script ran against | mitigate | Task 4 writes `region.log` capturing the region used and timestamp; SUMMARY.md (output) captures the project id |
| T-00-22 | Elevation of privilege | gcloud auth identity has more reach than the kill-switch SA | accept | Operator identity = solo dev account; required to set up the rest. Phase 6 (PORT-05) introduces Workload Identity Federation for CI but that's deferred per D-12 |

</threat_model>

<verification>
- All four checkpoint tasks complete with their resume signals
- Live GCP project exists with billingEnabled=True, kill-switch function ACTIVE, budget created with 50/90/100% thresholds, region pinned
- Kill-switch test passed on a throwaway project; PROD project is unaffected
- `region.log` created at `.planning/phases/00-bootstrap-cost-guardrails/region.log` capturing the chosen region with a timestamp
</verification>

<success_criteria>
ROADMAP Phase 0 success criteria #2 (GCP project + budget + alerts + single region) and #3 (kill switch deployed AND tested end-to-end) are satisfied. BOOT-02, BOOT-03, BOOT-04 close. The live GCP project sits idle (no Cloud Run, no GCS, no BQ — those start in Phase 3) but is ready when Phase 3 begins.
</success_criteria>

<output>
Create `.planning/phases/00-bootstrap-cost-guardrails/00-07-SUMMARY.md` when done. Record (a) the live GCP project id, (b) the chosen region, (c) the throwaway project's fate (deleted or left disabled), (d) the kill-switch test runtime in seconds, (e) the exact gcloud version used. This SUMMARY is the source of truth for Phase 3+ when they connect to this project.

The SUMMARY.md MUST NOT contain the billing account id, the operator's email, or any service-account JSON content — only the public-safe project metadata. (`scripts/bootstrap-gcp.env` remains gitignored.)
</output>
